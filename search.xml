<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[2018总结]]></title>
    <url>%2F2019%2F01%2F31%2F2018%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[2018 涵盖了大三大四两个学期，在整个大学 4 年中对我来说意义比较重大，事情也比较多，总觉得应该写点什么 ，也能作为后面的回忆吧。 带队刷怪大二大三在校实验室呆了两年，算是前端开发的入门阶段。实验室只是学校的统称，我们实验室更确切的应该算是一个工作室。我在大二阶段就在学长学姐的带领下学习 web 开发相关的内容，大三了就带学弟学妹开发… 实验室维护着一个核心项目拇指课堂，因为前端技术栈比较落后，加上当时写的比较混乱，已经在不可维护的边缘了，所以导师找我们商量着更新一下技术栈（ionic1 到 ionic3）这个更新基本是重写…众所周知 Angularjs 和 Angular 是俩个框架（angular1.x 统称为 angularjs，angular2+统称为 angular，1 到 2 是个不兼容更新，基本可以认为 ng1 和 ng2 是两框架。后面的 ng2+ 都是在 ng2 基础上升级，是平滑更新），而 ionic1 用的 ng1，ionic3 用的 ng4。所以这套技术栈升级就是看着 UI 重写一遍… 2018-01-22，在 thumbclass3.0 项目中提交了第一个 commit， 12394b9017 You, a year ago (January 22nd, 2018 1:25pm)Initial commit 随后，便用了一个寒假时间带着学弟学妹们一起将拇指课堂学生端完成了。整个过程按着 Angular 的官网给的最佳实践控制文件结构和代码风格。这段时间是我能力的一个小跨越（还有个大跨越是实习阶段），因为这个 thumbclass3.0 算是自己从 0 开始的项目，开发中遇到的大大小小的问题也主要由我来处理，甚至一些概念也是我学完了再去教学弟学妹（Promise，Rxjs 等）。在给他们讲的时候自己对某些知识点的了解也会更深入（偶尔还会有他们提出一个问题也是我没考虑到的，大家一起 google 一起学习的情况）。在技术分享和项目的双重刺激下，我的能力也飞速上升，还荣获傅老师的称呼 23333 大三上开始就进行了教师端的改写，如果说学生端部分是一起学习一起进步的话，那教师端部分我就有点偏向管理。因为有学生端改写的经验，大家都已经轻车熟路，我的主要任务就从和大家一起完成项目变成组织大家更好更快地完成项目。教师端改写这几个月我组织大家每周进行技术分享，偶尔也开个项目会议了解下大家的进度（这时还没有软件过程的概念），遇到什么问题大家讨论解决有什么技术难题就 google 解决。。。在 ionic3 上也踩了不少坑，都记录在项目文档中（项目之初特地建了个 doc 目录用来放各种文档） 2018-07-02，在 thumbclass3.0 项目中提交了最后一个 commit， 1234ccc4da7 You, 7 months ago (July 2nd, 2018 6:58pm)修改dismissAll为dismiss修复页面多次跳转的bug 其实开发部分早就完成了，只是一直在做一些修修补补的事，比如补了一下项目交接的文档，补了一下 单元测试、e2e 测试，就是各种测试，包括一些自动化测试和手动点点点…修复了一些 bug 改善了一些体验上的问题，然后部署到了测试服，算是进入了内测阶段吧… 这一段带队刷怪的经历让我学到不少，尤其是解决问题部分，能熟练 google 出想要的答案，当然这也算一个不错的项目经历。另外，早就知道学校实习计划的我便早早地开始了准备，期间看了《JavaScript 高级程序设计》、《你不知道的 js》、《css 世界》等书，并在 3 月份就顺利获取了有赞实习的机会（提前占坑，7 月份去实习） 新的征程虽然学校给的实习时间是 9-12，但我 7 月就去有赞实习了，毕竟早点去就多学点。刚进有赞实习心情是很愉悦的，然后实习生就给配 mac 也让我小小的惊讶了下（后面我面了好多公司正式员工给配的也不多）。职场小白表示对一堆术语一脸懵逼比如 QA，pre，prd，然后一开始的环境配置也很让人绝望，还有各种权限申请之类的。比如测试服务器部署权限（测试是没空给你部署环境的，他们只管测），发布上线权限，代码仓库权限 balabala… 由于有赞技术栈是 PC：React、h5:Vue、小程序：原生、node：Astroboy（内部基于 koa 二次开发的框架），可以说完美避开了我的技术栈（Angular、express）所以在有赞的半年也是飞速上升的半年，不仅是项目经验上，更是技术上…比如一天入门 react 啦，一周就上手项目开发，顺便看完了 React 官网文档和《深入浅出 React 与 Redux》,虽然有赞内部（几乎）不使用 Redux，加上几乎是因为不限制，你爱用就用，大多数情况下没必要，因为 PC 上是多页中的单页架构，所以每个单页都不是很复杂。 我虽然是在 PC 组但是因为业务比较复杂，所以 10 月份开始我的工作重心就在 h5 和小程序上了（因为内部还是按业务分的，我是做会员相关的所以 h5 和小程序的个人中心也在我们这边了）于是又看着 Vue 和小程序官网把这两框架学了下。有一点要说明的是，有赞面试的时候问的都是 html/js/css，js 偏多也比较深入，框架相关的啥也没问（大概当时看我的简历完美避开了他们技术栈所以没问吧…），所以 React 啊 Vue 啊我都是从 0 学起。之前在实验室写了一年 Angularjs 一年 Angular，学别的框架还是比较轻松的，无非就是一些模版一些数据一些样式。就是 jsx 这个东西感觉比较新奇，别的也没啥… 然后就是各种会议，项目评审、技术评审、测试用例评审、周会、站会、项目发布计划回滚计划等。还会遇到一天白天都在开完晚上码会儿代码等情况比较难受…还会有各种线上 bug 报过来跟进，不是自己的再转给别人。很少有一大段时间可以用于开发，iterm/vscode 同时打开 4、5 个窗口是日常… 总结一下在有赞的收获， 了解了各个环境的区别以及必要性。开发环境，测试环境，预发环境，线上环境。然后有赞有个 zan-proxy 用于吧某个 url 对应的页面代理到本地，对于开发提供了极大方便。 熟悉了项目的开发流程，立项-项目评审-技术评审-排期-开发-测试用例评审-提测-预发验收-上线。 学会了看源码，学会了在 node_modules 里 debug。 扩充了下技术栈，前端三大框架也终于都有所涉及了，然后对 node 的使用更顺畅了，顺便写了点小程序。 对前端的技术架构有了更深入的理解，毕竟有赞开放几乎所有的代码仓库，所以有些底层框架的代码也看了很多。 未来虽然因为种种原因从有赞离开了，但这半年的实习是我提高最快的半年，大学的最后一学期就在做毕设找工作中度过了。2018 总结就是成长、充实。2019 虽然遇上寒冬但是春天也不会遥远。 最后给 2019 立几个 flag 吧 一个月看完一个 npm 包的源码，并留下笔记。 一季度看完一本书。 学习 webGL，做一个 3D 游戏出来。]]></content>
      <categories>
        <category>life</category>
      </categories>
      <tags>
        <tag>life</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[实习总结]]></title>
    <url>%2F2019%2F01%2F31%2F%E5%AE%9E%E4%B9%A0%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[在有赞共享前端实习了半年（18.7-19.1）总结下自我的收获和实习的体验。 首先是环境，用了 nvm 来控制 node 的版本，async/await 只有 node8+才能用，新的项目都统一用 8.x，旧的项目用的是 node6 的（node 端不过 babel 编译，直接运行）。第一次使用 mac 对命令行不是很熟，配了下 iterm2 和 ohmyzsh，发现 mac 下的命令行真好用…原本 IDEA 的深度用户的我在接触了 vscode+iterm2 后表示真香～后来就是日常 iterm2 分 4 屏然后开 3-4 个 vscode。。。 然后是各种账号权限，本地生成了 rsa 加到 keychain，然后 ssh 连服务器，服务器环境有开发、测试、预发、线上，一般情况下用专门的发布平台来部署到对应的机器，极端情况需要直接 ssh 连服务器调试代码。内部系统比较多，包括代码仓库、文档系统、bug 跟踪、发布系统、项目跟踪、线上监控等，刚接触的时候一脸懵逼，每个平台都深入使用过才了解。有些平台是内部开发维护的，有些是购买的比如 jira。每个系统都有自己的功能，不同系统用在开发的不同阶段，提高效率。 我在 PC 组，一开始主要是熟悉开发套路，所以就是改改文案改改样式，把整个收到 bug 到修复 bug 到上线的流程走一遍。PC 端用的 React 全家桶，在学校时我主要写 Angular，所以赶紧花时间把 React 官网过了一遍。接手的第一个项目是 UI 改写。有个单独的平台原本是前端凭感觉做的，现在设计给了稿子，就需要把整体 UI 按照设计给的稿子做一遍。因为使用了 style-components，所以顺便学习了一下 css in js 的套路，感觉对组件的抽象要求比较高，有些样式有多种方案的需要考虑组件复用性高的方案。通过这个项目，对 React 开发也算入了门。后面几个项目涉及到数据请求，以及逻辑处理，更加深了对 React 的理解，期间看了《深入浅出 React 和 Redux》是我受益匪浅。然而有赞 PC 是多页中的单页，由 node 渲染基础模版，然后留一个 div 给 react 渲染组件。所以每个单页应用不是很复杂，也没必要使用 Redux 了。最后前端代码打包完上传到 cdn，留一个 hash 塞入 node 层模版中，所以 node 层也可以直接塞一些数据在 global 下，可以节省一些异步请求。 因为按业务划分，我是做会员业务的，所以 h5 和小程序的个人中心后来也从移动组划到了我们这里（据说是因为人不够？？）于是我就开始写 Vue 了。。。（h5 是 Vue 全家桶）主要就是一个个人中心页面。UI 上还好，没有什么比较骚的操作，有意思的地方在整体页面样子要根据 PC 后台的配置来，整个就是店铺装修的套路。另外 PC 端还需要展示预览效果，所以需要把整个页面分拆成组件后打包发布，以便于 PC 端使用。h5 要根据 PC 的配置比如主题色、组件位置、插件显示模式等。所以整个使用了 Vue 的动态组件，从后端读取 PC 上修改的页面配置，根据配置渲染不同的组件，对于有些组件需要另外的参数控制显示效果也是从配置中读取的。读取完配置后初始化 Vuex 的 store，UI 组件再根据 Vuex 的 state 渲染不同的样式。打包给 PC 的那份组件在提供默认的 Store。另外 PC 端在 React 里渲染 Vue 的组件个人感觉也比较骚，然而遗憾的是米有机会看他的源码就离职了。。。 然后是个人中心页面的小程序版本，UI 上和 h5 一致，技术上也没啥难点，就是体验了下小程序开发，小程序整体开发体验上和 shadowDOM 比较相似，但是因为微信提供的小程序框架基本上是个黑盒子，有些 api 的错误也很难查找原因，只能跟微信的人咨询，这种感觉就很蛋疼。在开发中曾经出现过一个调用微信的 api 报没有权限的 error，搞了好久才弄好，真是令人心力憔悴。。。 业务部分讲完了，讲讲技术部分把，也站在上层回顾下有赞前端的技术架构。首先，后端是基于 dubbo 的微服务架构，然后内部用 go 实现了一个代理框架 Tether 暴露一个 http 服务，微服务相关的部分都由 Tether 来处理（服务发现服务调用负载均衡等），暴露的 http 服务是让 node 调用的，比如 node 发一个 POST www.demo.com:6666/i.am.a.long.package.name/iAmAServiceName.functionName 到 Tether，Tether 会解析这个请求，然后转发到对应包名类名方法名的 java 接口（参数视 java 借口而定）并把 java 返回的数据过一层解析和封装，变成{code:200,data:{k:666},message:&#39;xxx&#39;}的形式返回给 node。前端部分是 node + React/Vue 的结构，node 部分是基于 koa 分装的 Astroboy 框架，已开源。在 Astroboy 基础上包装了业务层框架 youzan-framework。加了底层把业务方法调用改成 http 调用的形式。比如上面的 POST www.demo.com:6666/i.am.a.long.package.name/iAmAServiceName.functionName，开发者只需要写 this.invoke(&#39;i.am.a.long.package.name.iAmAServiceName&#39;,&#39;functionName&#39;,[param1,param2])youzan-framework 除了做方法解析，还针对 http 调用做了一些设置比如超时或者一些特定请求头等。在 youzan-framework 之上又针对不同的业务线（toB、toC）封装了两个不同的上层业务框架。因为有赞的业务 toB 部分基本是 PC 后台，toC 部分就是 h5 相关，所以这两业务框架基本 PC 的一套 h5 的一套，里面封装了不同的登陆态获取方式等。当然也有 toB 的 h5 页面（我当时做过一个），所以就会出现一个仓库既有 React 又有 Vue 的尴尬…但是他们基本不相干，就加两份打包配置而已…这三个框架都属于 node 层底层框架，业务开发基于他们进行。前端页面部分整体不是一个 SPA 而是多页中的单页。node 会渲染页面模板给浏览器，同时预留一个类似&lt;div id=&#39;app-root&#39;&gt;&lt;/div&gt;给 React/Vue 来挂载。然后对于 PC 端部分，整个一级导航的每个项都有一个代码仓库，用 nginx 针对不同 url 来转发。可以做到独立开发独立部署。一个代码仓库负责一个一级导航下的整个二级导航。基本上每个二级导航都对应了 node 层一个模板页面，对于有三级导航的大多是 SPA 了。三级导航的 router 就由前端来控制。前端页面开发完后，会把 webpack 打包结果上传到 cdn，并把 hash 值写入配置文件。node 层渲染模板的时候会去读取配置文件的 hash 值，拼接成 cdn 的完整 url，并加到模板的 script 标签上。当然这一切都有 cli 负责整个流程的自动化进行。 最后讲讲有赞的技术积累。它有 zan-proxy、zan-api 等提高开发效率的工具也有 zent、vant 等组件库，还有比较丰富的文档网站。可以说在积累上是比较富裕的。zan-proxy 主要用于代理某些请求到本地，之前提了有赞是多个仓库通过 nginx 代理的，所以不可能本地跑整站的代码，就可以把具体业务部分的页面代理到本地的 node 服务进行调试开发。当然直接只运行这部分页面在本地开发也是可以的，但是有些需要依赖 cookie 等，使用代理可以更好的贴近线上的模式。它的原理是通过配置正则，符合某个正则模式的请求就会代理到本地某个端口暴露的服务。具体点比如 www.baidu.com 的 GET 请求代理到 localhost:8080 的 node 服务，这样别的还是会走线上的，进入到符合正则匹配的 url 时就走本地的了。zan-api 主要用于前后端接口定义，配合 zan-proxy 可以实现前端通过接口测试后联调时几乎不用再改前端代码，当然具体的 mock 数据需要自己编写。具体原理是某个接口的请求就走到一个统一的 mock 服务上，返回 mock 数据，整个过程处理数据是 mock 的别的完全一致，所以等后端开发完后只需要把这个接口的转发关掉就走到了后端那里。这两个工具个人感觉都比较使用，整体原理上也不难理解。可以较大程度提高开发效率。]]></content>
      <categories>
        <category>life</category>
      </categories>
      <tags>
        <tag>life</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Rxjs简介]]></title>
    <url>%2F2018%2F03%2F18%2FRxjs%E7%AE%80%E4%BB%8B%2F</url>
    <content type="text"><![CDATA[谈谈Rxjs库的基本原理和使用。 Rxjs库Rxjs是时下比较受欢迎的RP范式的一个库，RP是响应式编程的意思，就是说对于c=a+b，a，b的变化均会导致c的变化。 1234let a = 1;let b = 2;let c = a+b;//3a = 10;//c还是3，就不是响应式的 换成响应式的 12345678let a = new Rx.Subject();let b = new Rx.Subject();let c = a .combineLatest(b, (total, now) =&gt; total + now) // a+b .subscribe(console.log);a.next(1);b.next(2);// 输出3a.next(10);// 输出12 这里需要引入Rxjs的库，那么什么是Rxjs呢？应用官网的话“RxJS 是使用 Observables 的响应式编程的库，它使编写异步或基于回调的代码更容易。”emmmmmm…说和没说一样。。。那我用自己的理解说一下，Rxjs大概就是把一切数据都看做是流，然后所有的操作都是对流的变幻，每当流里面出来一个新的值都会经过一系列预设的操作得到相应的结果。应用响应式的思想可以让结果同步变化，应用函数式的思想可以让流处理变得十分方便。 12345678910let a = new Rx.Subject();let b = new Rx.Subject();let doublea = a.map(d =&gt; d * 2); //2*alet squareb = b.map(d =&gt; d * d); //b^2let c = doublea .combineLatest(squareb, (total, now) =&gt; total + now) // 2*a+b^2 .subscribe(console.log)a.next(1);b.next(2); //输出6a.next(10); //输出24 这里的map，combineLatest都是流处理函数，最后的subscribe是流订阅函数，只有被订阅的流才可以真正触发（订阅就类似于函数调用，没有订阅的流就只是一些步骤的定义，只有订阅了流才工作） 上面说了一堆就是Rxjs入个门，Rxjs最大的魅力就是丰富多样的流处理函数，对于那么多的处理函数官网的查询机制也很完善，可以去体验下。 对于Angular中，是默认使用Rxjs库的，比如常用的HTTP请求。我们先对aaa发起一个get请求，然后json化后取出bbb字段，最后log出来 1234this.http.get('aaa') .map(data=&gt;data.json()) .map(data=&gt;data.bbb) .subscribe(console.log); 进一步学习30 天精通 RxJS使用响应式编程来实现简易版的无限滚动加载Rxjs官网]]></content>
      <categories>
        <category>front-end</category>
      </categories>
      <tags>
        <tag>front-end</tag>
        <tag>Rxjs</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[函数式编程]]></title>
    <url>%2F2018%2F03%2F18%2F%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B%2F</url>
    <content type="text"><![CDATA[谈谈函数式编程在js的实践。 函数式编程是什么首先讲讲编程范式，所谓编程范式就是代码世界的方法论。大家比较熟的就是面向过程和面向对象了，这些范式并不是锁定语言的，但是有些却是需要语言特性支持。比如c就是常见的面向过程的编程范式语言，但是因为指针、结构体的存在让它也具有一定的面向对象的能力。java是常见的面向对象的语言，但是要是写类和方法时没有封装，继承的思想，那也不过是面向过程包上了一层class的外衣。 以上解释了范式不限定语言，那何为范式需要语言支持呢？这就提到函数式编程了，函数式编程范式的思想是把一切行为都用函数连接起来，每个函数都有确定的输出，整体就是一个y=f(x)的形式，扩展下就有y=f(g(t(x)))这样一层一层嵌套的函数调用。写的优雅一点就是gg=g(t),ff=f(g),y=ff(x),可以看到这里把函数t作为函数g的参数了，然后组合出了新的函数gg，这里可以引出函数式编程的要求是具有吧函数作为参数传递的能力，官方点说法就是“函数是一等公民”。自然，这个也不是必要条件，只是个充分条件（函数是一等公民一定可以函数式，函数式不一定要函数是一等公民，但是需要多绕几个弯比如C++可以重载类的函数调用操作符就可以创造函数对象这种东西就可以做参数传递了）另外，函数式是一种思想，我们学习只是为了提高某些代码的简洁性和优雅性，不能为了函数式而函数式编程。 为什么扯了那么多，那函数式编程有啥用呢？为了提高某些代码的简洁性和优雅性。。。废话，具体点？传统编程中会有需要状态管理以及处理中间变量的情况，比如求和，那就需要一个sum变量，然后手动遍历数组每个数字并加到sum里面。恩很轻松就搞定了。那需要正负分开求和呢？那就加个if。。。需要求平方和呢？当然这个例子有点钻牛角尖，其实我想说明的是，随着需求的变多我们需要申明更多的变量来存储这个结果，一个大型的系统，变量越来越多状态也会更加复杂，就会趋于不可控，于是就出来很多状态管理机制比如有限状态机之类的。但是函数式的思想就是记录过程不记录结果，关注点从每一步做法转移到每个过程的拼接。好处就是定义一些基本函数后，有新的需求就把基础函数组装起来就可以了，比如arr.sum(), arr.bigthan0().sum(), arr.smallthan0().sum() 上面只是简单提了状态多带来的副作用（不好管理），函数式编程真正的优势是没有中间状态就可以不用担心状态被修改或者出现竞争导致的多线程下各种复杂问题，所以可以轻松应付高并发环境。 怎么做函数式的一些基本概念 函数没有副作用，就是所谓的纯函数。即函数对同一输入有相同输出，且不会修改外界的状态。 函数可以作为参数传入别的函数并在需要的时候调用，也可以从函数返回新的函数。就是所谓的高阶函数。 多个参数的函数可以进行参数拆解，返回新的函数（需要比较少的参数）就是所谓的科里化。科里化可以更好地函数复用。 js中函数是一等公民，它是天生的支持函数式编程的语言，es6中箭头函数的引入又在写法上带来了便捷性（箭头函数还有更深的特性比如不产生this，只不过与这个主题无关），另外箭头函数的写法各个语言都有一定的支持（就是大名鼎鼎的lambda表达式）。然后js的Array对象有很多支持函数式的内置函数也给函数式编程带了很多便利，比如map，filter，reduce等。 比如花样求和 1234const arr = [1,4,9,5,2,6,-2,-3,-7];arr.reduce((sum,item)=&gt;sum+item); //求和arr.filter(x =&gt; x&gt;0).reduce((sum, x) =&gt; sum + x); //正数求和arr.map(x =&gt; x*x).reduce((sum, x) =&gt; sum + x); //平方和 可以看到就是map，reduce，filter的简单组合 123const sum = (a,b) =&gt; a+b;const fun = (f,a,b) =&gt; f(a,b)fun(sum,1,2) //3 进一步学习函数式编程入门教程如何读懂并写出装逼的函数式代码(看看长长见识就好了，千万别写出这种代码…)]]></content>
      <categories>
        <category>front-end</category>
      </categories>
      <tags>
        <tag>front-end</tag>
        <tag>FP</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[谈谈跨域]]></title>
    <url>%2F2018%2F01%2F16%2F%E8%B0%88%E8%B0%88%E8%B7%A8%E5%9F%9F%2F</url>
    <content type="text"><![CDATA[详解前端跨域的概念和解决方案。 何为跨域，为何跨域？何为跨域 一言以蔽之： 只要协议、域名、端口有任何一个不同，都被当作是不同的域，之间的请求就是跨域操作。 比如http与https，80端口与81端口，a.com与b.com以上三个情况的各种组合都是不同域的，互相之前的请求就是跨域请求 浏览器同源策略那所谓的跨域是怎么产生的呢？那么就要提到同源策略了。浏览器为了安全考虑就制定了一个同源策略，同源策略又分为以下几种 DOM同源策略：禁止对不同源页面DOM进行操作。这里主要场景是iframe跨域的情况，不同域名的iframe是限制互相访问的。 XmlHttpRequest同源策略：禁止使用XHR对象向不同源的服务器地址发起HTTP请求。就是所谓的ajax请求，生活中这个场景较多见。 这里主要介绍ajax跨域的情况。 另外，img，script，link，iframe等标签的src都是不受同源策略约束的，可以自由引用不同源的内容。想想要是没有同源策略，a网站上的js就可以操作b网站上的DOM并且可以发起请求，这将是一个灾难！所以一切为了安全… 同源策略更详细的介绍： 浏览器同源政策及其规避方法 同源策略详解 为何跨域总有这样那样的原因使我们想要请求别的网站的数据或者和别的网页交互。这个原因可以是 前后端分离开发，前端用了webpack-dev-server必然有个和后端不同的端口… 想要获取其他网站的内容比如教务处请求课表啊成绩啊之类的 跨域解决方案首先明确一点对于端口和协议的不同，只能通过后台来解决。 既然提到解决方案当然要上代码，但是作为demo代码比较简略。后端使用nodejs的Express，浏览器用chrome。本地服务器模拟不同域名最简单粗暴的方式是改host。。。文件位于C:\Windows\System32\drivers\etc右键hosts用记事本打开，里面长这样 123456789101112131415161718192021# Copyright (c) 1993-2009 Microsoft Corp.## This is a sample HOSTS file used by Microsoft TCP/IP for Windows.## This file contains the mappings of IP addresses to host names. Each# entry should be kept on an individual line. The IP address should# be placed in the first column followed by the corresponding host name.# The IP address and the host name should be separated by at least one# space.## Additionally, comments (such as these) may be inserted on individual# lines or following the machine name denoted by a &apos;#&apos; symbol.## For example:## 102.54.94.97 rhino.acme.com # source server# 38.25.63.10 x.acme.com # x client host# localhost name resolution is handled within DNS itself.# 127.0.0.1 localhost# ::1 localhost 把测试的几个域名指向本地，也就是在这个文件的最后面加上以下几行内容 123127.0.0.1 www.aaa.com127.0.0.1 www.bbb.com127.0.0.1 aaa.com 简单解释就是浏览器输入后面的三个网站时会默认打开本地的服务器(输网址记得加上http://，不然浏览器可能默认会加上www…)。测试完之后删了就好了，别的没什么影响。需要注意的是如果有ss或者ssr记得关了它，chrome走代理的话就白改了，看不懂这句话请跳过。 然后我们需要知道的是，所谓的跨域是不管你这个域名对应的ip是啥，只看域名，对不上就跨域（就是个字符串处理），所以可以看到我们改了host文件吧三个域名都对应到了本地服务器来实现多个域名。代码在这里，当然本文中我也会贴代码。。。 host的不同源环境配置完成，现在我们要打代码了，就是发几个跨域请求…新建个文件夹crossorigin，命令行进去输入npm init -y初始化项目，然后yarn add express安装依赖新建src文件夹，然后建立相应文件并写代码。 src/server1.js 1234567891011const express = require('express');const app = express();const path = require('path');app.use(express.static(path.join(__dirname,'./public')))app.listen(8080)app.get('/iv1/hello',(req,res,next)=&gt;&#123; res.send('hello server 1')&#125;) src/server2.js 123456789const express = require('express');const app = express();const path = require('path');app.listen(8081)app.get('/iv1/hello',(req,res,next)=&gt;&#123; res.send('hello server 2')&#125;) src/public/index.html 1234567891011121314151617181920212223242526272829303132333435363738394041&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt; &lt;meta charset="UTF-8"&gt; &lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt; &lt;meta http-equiv="X-UA-Compatible" content="ie=edge"&gt; &lt;title&gt;跨域&lt;/title&gt;&lt;/head&gt;&lt;body&gt; &lt;button onclick="ajax1('http://www.aaa.com:8080/iv1/hello')"&gt;www.aaa.com:8080&lt;/button&gt; &lt;button onclick="ajax1('http://aaa.com:8080/iv1/hello')"&gt;aaa.com:8080&lt;/button&gt; &lt;button onclick="ajax1('http://www.bbb.com:8080/iv1/hello')"&gt;www.bbb.com:8080&lt;/button&gt; &lt;button onclick="ajax1('http://www.aaa.com:8081/iv1/hello')"&gt;www.aaa.com:8081&lt;/button&gt; &lt;button onclick="ajax1('http://127.0.0.1:8080/iv1/hello')"&gt;127.0.0.1:8080&lt;/button&gt;&lt;/body&gt;&lt;script&gt; function ajax(url) &#123; let xhr = new XMLHttpRequest() xhr.open('GET', url) xhr.onload = e =&gt; &#123; if (xhr.status === 200) &#123; console.log(xhr.response) &#125; else &#123; console.log(xhr.status) &#125; &#125; xhr.send() &#125; function ajax1(url) &#123; fetch(url) .then(res =&gt; res.text()) .then(data =&gt; &#123; console.log(data) &#125;) &#125;&lt;/script&gt;&lt;/html&gt; 代码很简单，js部分开了两个服务器监听8080，8081端口，并用Express的static设置了静态目录，（就是说访问localhost:8080就是显示public/index.html）然后后端挂了个/iv1/hello的get接口，返回字符串‘hello server 1’，‘hello server 2’，html的代码就是放了几个按钮用于发起ajax请求，这里写了xhr和fetch两个方式，这是目前主流两种ajax方式。好了，在package.json的scripts里面加两个命令”server1”: “node ./src/server1.js”,”server2”: “node ./src/server2.js”，并执行npm run server1，npm run server2。访问http://www.aaa.com:8080/ 这个域名等效于localhost:8080 。因为我们host做了映射直接访问127.0.0.1了。打开控制台看console界面，然后依次点击5个按钮，可以看到，除了点第一个按钮出来了结果，其他几个都是报错说我们跨域了。那么，我们的旅程现在开始。 CORS可以看到主要的报错信息是这样滴，上面那个是fetch的报错信息，下面是xhr的报错信息。 Failed to load http://aaa.com:8080/iv1/hello: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. Origin ‘http://www.aaa.com:8080&#39; is therefore not allowed access. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled. Failed to load http://aaa.com:8080/iv1/hello: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. Origin ‘http://www.aaa.com:8080&#39; is therefore not allowed access. 划重点Origin &#39;http://www.aaa.com:8080&#39; is therefore not allowed access.是说 “http://www.aaa.com:8080” 这个源不被允许访问“http://aaa.com:8080/iv1/hello”。 那么No &#39;Access-Control-Allow-Origin&#39; header这是啥呢？它是说服务器上没有设置过这个头字段(header)。这也就引出了CORS的概念。CORS是一个W3C标准，全称是”跨域资源共享”（Cross-origin resource sharing）阮一峰的这篇文章把CORS的细节讲的十分透彻了，我这里就不赘述，我就简单提一下代码怎么写，同时你也会明白怎么就莫名其妙的引出CORS了。 说出来你可能不信，代码十分简单。。。 在src/server1.js的app.get之前加上以下代码 1234app.use((req,res,next)=&gt;&#123; res.setHeader('Access-Control-Allow-Origin','*') next()&#125;) 是不是看到了熟悉的’Access-Control-Allow-Origin’。其实这就是说web官方看你们那么想跨域，就制定了一个标准，要是后端的响应头设置了’Access-Control-Allow-Origin’这个字段，那么就允许你们跨域请求这个接口。当然，一般情况一行代码不够用，emmmm…一行不够就三行喽。 123456app.use(function (req, res, next) &#123; res.setHeader("Access-Control-Allow-Origin", "*"); //允许哪些url可以跨域请求到本域，*表示全部 res.setHeader("Access-Control-Allow-Methods", "GET, POST"); //允许的请求方法，一般是GET,POST,PUT,DELETE,OPTIONS res.setHeader("Access-Control-Allow-Headers", "x-requested-with,content-type,Token"); //允许哪些请求头可以跨域 next();&#125;); 注释很详细啦~，一句话总结：CORS是为了跨域制定的标准，只要后端设置了响应头，符合要求的请求就可以跨域请求成功。总结完加个ps：CORS需要浏览器本身支持，但是现代浏览器都是支持的，所以就提一下。（IE已被现代浏览器除名…） pps：CORS默认不会带cookie，要带cookie的话js和服务端都要做额外设置。。。对于客户端，我们还是正常使用xhr对象发送ajax请求，但是要设置： 1xhr.withCredentials = true; 对于服务器端，需要在 response header中设置如下两个字段（注意第一个必须是具体域名，不能是*）: 12Access-Control-Allow-Origin: http://www.yourhost.comAccess-Control-Allow-Credentials:true jsonp相对于CORS有标准的支持，jsonp可以说只是一个小技巧。只不过这个技巧用多了就发现挺顺手然后大家都用了。。。前文提到scrip标签的src引用的内容不受同源策略限制，那我们就手动创建一个script标签发起一个get请求，并且把某个函数名f作为参数传过去，并在全局定义一个f函数。后端返回一串字符串，字符串的形式是函数调用”f(data)”，script标签请求完后直接执行js就会执行f(data)，然后就完成了跨域数据请求。 好吧，是有点绕。。。我们实践下，这次打开server2.js，之前CORS的代码写到了server1，server2是无法跨域的。 src/server2.js 123456789101112131415161718const express = require('express');const app = express();const path = require('path');app.listen(8081)app.get('/iv1/hello', (req, res, next) =&gt; &#123; res.send('hello server 2')&#125;)app.get('/iv1/jsonp', (req, res, next) =&gt; &#123; const funName = req.query.funName const data = &#123; a: 1, b: [1, 2] &#125; res.send(`$&#123;funName&#125;($&#123;JSON.stringify(data)&#125;)`)&#125;) public/index.html最后在加两个按钮，然后script里面加个函数。（第一个按钮是测试跨域的） 1234567891011121314151617181920...&lt;body&gt; ... &lt;button onclick="ajax('http://www.bbb.com:8081/iv1/hello')"&gt;get www.bbb.com:8081&lt;/button&gt; &lt;button onclick="jsonp()"&gt;jsonp www.bbb.com:8081&lt;/button&gt;&lt;/body&gt;&lt;script&gt; ... function jsonp() &#123; const url = 'http://www.bbb.com:8081/iv1/jsonp?funName=logData'; window.logData = function (data) &#123; console.log(data) &#125; const script = document.createElement('script'); script.setAttribute('src', url); document.querySelector('body').appendChild(script); &#125;&lt;/script&gt;&lt;/html&gt; 然后访问http://www.aaa.com:8080/，点击最后两个请求，可以看到第一个被浏览器拦截说是跨域了，第二个正确log出了数据。 jsonp大概就是这样，人为伪造一个script标签发起请求并返回函数包裹数据的形式，前端拿到返回值直接当做js代码执行了。jsonp在CORS出现之前可是顶了半边天。。。但是它的限制也很大，首先最致命的事只支持get请求，毕竟本来就只是script标签的src属性发起的请求。。。然后就是后端要为jsonp类型的请求专门写一个接口。因为正常接口直接返回数据的话jsonp的接口需要返回函数名包裹数据的形式。 后端代理这个方法就比较简单粗暴了。首先明确一点是一个简单请求虽然跨域了但是他是真的到达后端的，跨域的报错是浏览器拦截了那个请求的返回。对于非简单请求，浏览器会发一个方法为OPTIONS的请求，主要是去问问服务器是否允许跨域，不允许的话浏览器就报错了，允许的话浏览器就会发起正式的请求。可以看到，这一切都是浏览器的锅。。。所有的跨域都是浏览器的规则，那么就意味着我服务端发送的请求时可以得到正常响应的，于是就有了如下骚操作：host a的网页请求host a的某个接口，那个接口去请求host b的某个接口，并把请求的结果告诉host a的网页（浏览器）。对浏览器来说一切都是和自己家的后端交互，至于跨域请求那就让后端帮我们去请求并把结果告诉我就好啦，这就是所谓的代理。。。 上代码 1234&lt;body&gt; ... &lt;button onclick="ajax('http://www.aaa.com:8080/iv1/proxy')"&gt;www.aaa.com:8080后端代理www.bbb.com:8081&lt;/button&gt;&lt;/body&gt; src/server1.js 1234567891011121314...const http = require('http');...app.get('/iv1/proxy', (req, res, next) =&gt; &#123; http.get('http://www.bbb.com:8081/iv1/hello', function (_req, _res) &#123; let data = ''; _req.on('data', function (chunk) &#123; data += chunk; &#125;); _req.on('end', function () &#123; res.send(data); &#125;); &#125;);&#125;) 可以看到html部分就是多了个按钮向自己的后端的代理接口发了个请求，后端首先引入了http库（nodejs自带）然后用http向目标url发了个get请求并在请求完成后把数据返回给前端。点击最后一个按钮可以看到数据成功打印在了控制台。 这个后端代理的好处就是完全不管啥同源策略什么的，暴力请求，请求玩数据扔给前端就行，emmmmm本质上就是个爬虫…]]></content>
      <categories>
        <category>front-end</category>
      </categories>
      <tags>
        <tag>front-end</tag>
        <tag>跨域</tag>
        <tag>jsonp</tag>
        <tag>CORS</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[安卓调试数据库]]></title>
    <url>%2F2017%2F11%2F15%2F%E5%AE%89%E5%8D%93%E8%B0%83%E8%AF%95%E6%95%B0%E6%8D%AE%E5%BA%93%2F</url>
    <content type="text"><![CDATA[安卓sqlite的web可视化，便于调试时对数据进行操作参考链接 在 Android Studio 上调试数据库 ( SQLite )Issuues Open http://0.0.0.0:8080 in your browser 第一个链接中，作者给了两个方案，一个是使用Android Studio的一个收费插件SQLScout，我等贫民直接跳过，第二种是一个开源项目Android Debug Database，可以在网页上打开查看并操作数据库，使用方法如下：先在 module 的 build.gradle 里面添加（加在app/build.gradle的dependencies里面）：debugCompile &#39;com.amitshekhar.android:debug-db:1.0.0&#39;让手机和电脑处于一个局域网下，当项目跑起来的时候，在 logcat 里面会打印出这么一行：D/DebugDB: Open http://XXX.XXX.X.XXX:8080 in your browser然后把地址复制到浏览器打开就好了。 然后我就遇到了一个问题，logcat输出的是0.0.0.0:8080，那我就很尴尬，去搜索了下AS3好多人都有这个问题，并在那个项目的Issues里找到了解决方案（具体的就是第二个链接）打开命令行，运行adb forward tcp:8080 tcp:8080然后打开浏览器输入 http://localhost:8080 就好啦要是运行上面命令出现问题： 没把安卓sdk加入环境变量的进入sdk安装目录的platform-tools里执行 出现重定向拒绝或者说没权限的可能是端口冲突了，吧第一个8080改成别的数字比如8081，然后浏览器输入的也对应的改为8081就好了。]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Android Studio</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[MEAN stack的点点滴滴]]></title>
    <url>%2F2017%2F11%2F03%2FMEAN-stack%E7%9A%84%E7%82%B9%E7%82%B9%E6%BB%B4%E6%BB%B4%2F</url>
    <content type="text"><![CDATA[一个简单的关于MEAN.js的分享（入门教程） 总览写在前面：Angular自从出2.x版本后1.x就统称为Angularjs，2以及后面的版本称为Angular另外Angularjs将在未来停止更新，本文基于Angularjs，所以前端部分的写法不用深究，其他内容换成Angular后也可以使用。 MEAN.js简介MEAN是一个以JavaScript语言主导的web开发技术栈。是MongoDB、Express、AngularJS、Nodejs的首字母缩写的组合。前端部分为Google提供的开源框架AngularJS（2.0开始做了不兼容升级并改名为Angular，这里以AngularJS为例，因为我们实验室主导项目基于AngularJS的，还没完成升级。）后端部分用Nodejs自带的http模块开启一个服务器，并用Express框架提供的中间件设计完成后端逻辑。其中数据持久化使用了一种noSQL数据库MongoDB。 AngularJS是一个前端MVC框架，主要用于做单页应用（SPA，Single Page Application）。单页应用，顾名思义就是只有一个页面，里面所有的内容都是通过js动态请求数据并动态修改HTML的DOM实现的。 Nodejs是JavaScript的一个运行环境，由Google的V8引擎提供了js解析，让js可以脱离浏览器运行，并提供了一些系统api比如文件读写（fs模块），内置了一个http模块提供服务器支持（C++编写，效率保证）我们要使用它需要安装，网上可以找到安装程序，装完后可以通过node -v查看版本 Express是后端框架，作为Nodejs http模块的server中间件处理分发请求。 MongoDB是文档型数据库，内部数据为json结构，不用再进行对象的二次转换，使用很方便，效率也蛮高，比较适合数据变化较大，数据结构较复杂的网页。 以上只是极简单的介绍，详情可以自行参考各框架的官网~ 以一个小项目为例接下来以一个时间表小项目为例，记录下从０开始的编码过程。这个项目主要用于记录一周７天ｘ上午下午晚上３个时间段的空闲时间（实验室要求每个人规划自己的空闲时间提交来实验室的时间）。核心就两个模块，时间表模块和用户模块。时间表就增改查，用户就登录注册。出于演示所有功能逻辑都只是一个雏形．．．然后说一下我的环境，为了适应生产环境，我本地环境是node6.10.0，npm3.10.10至于npm是啥下文会讲。 后端Nodejs建立服务器首先提一下模块系统，我们自己开发的网页会用到很多别人写的库或者框架，要是全部以源码方式交流（git等方式团队合作）代码文件就很大，而且满大街找别人写好的东西也很累是不（笑），所以就有了模块系统也就是npm这个东西。在安装完nodejs后npm也一起装完了，npm就是一个nodejs的模块管理工具，官方一点就是包管理器。你需要下载一个什么东西直接npm install xxx就好了，是不是超级方便，执行完后当前目录下（当前目录就是你命令行执行那个命令的目录，“&gt;”左边那个东西，windows下按win+r输入cmd就打开了命令行，默认在用户目录“C:\Users\你的用户名”）就会有一个node_modules文件夹，下载的所有库都会在那里面。那么要是团队合作怎么知道我装了什么库呢？一般人都能想到，写一个文件记录装的库名字和版本号不就行啦，这就是package.json文件。当然package.json文件记录了更多的信息，比如项目名字，作者，开源协议等稍后再细讲。 好了进入正题。win+r输入cmd进入命令行(一个黑框框)，输入cd Desktop回车，工作路径切换到桌面（下文“执行xxx”就表示命令行输入命令后回车）执行mkdir timetable，桌面会新建一个timetable文件夹。执行cd timetable，npm init，然后会问我们好多问题，暂时全部保持默认一路回车（之后再改）。最后出现Is this ok? (yes)回车之后timetable文件夹就出现了package.json文件。我的文件长这样 1234567891011&#123; "name": "timetable", "version": "1.0.0", "description": "", "main": "index.js", "scripts": &#123; "test": "echo \"Error: no test specified\" &amp;&amp; exit 1" &#125;, "author": "", "license": "ISC"&#125; 当然刚刚那一堆命令也不用那么烦，你完全可以把上面这段代码复制，本地保存为package.json然后新建个timetable文件夹吧package.json扔里面，效果完全一样。 package.json是nodejs项目的模块配置文件，执行npm相关命令的时候都会读取或者写入这个文件，上文提到的把依赖的库名称和版本号记录下来也是在这个文件里面。在timetable文件夹下新建www.js，编写如下代码 123456const http = require('http');const server = http.createServer(function (req, res) &#123; res.write('hello nodejs'); res.end();&#125;);server.listen(8080); 回到命令行（希望你没有关，关了也没事，打开timetable文件夹，在不选中任何文件的情况下按住shift后按鼠标右键会发现右键菜单多了“在此处打开命令窗口”的选项，也可能是“在此处打开powershell窗口”…win10不太一样，点一下就打开在timetable目录的命令行了，当然你要通过cd切换我也没意见）执行node www，然后打开浏览器输入http://localhost:8080就可以看到输出了。这样就用nodejs很简单的建立了一个本地服务器，监听8080端口。 后端基本结构这样虽然开启了了服务器，并且也可以成功收到请求了，但是若是所有的请求都在一个函数里面执行逻辑，比如判断请求参数调用不同函数，那将是一个灾难（虽然用一个map似乎也不是不可以，但是我们有更好的方案）。然后执行npm i --save express引入express框架（如果你的命令行正在执行node你可以选择ctrl+c终止或者另外再开一个命令行）。这里i就是install的缩写，--save就表示吧这个库连同版本号一起写入package.json文件中。下载完依赖后打开package.json，会发现多了 123"dependencies": &#123; "express": "^4.16.2"&#125; 这就记录了这个项目依赖express这个框架（库），版本号为4.16.2。这样会安装最新版本，如果要装确定的版本，就在库名后面加上@版本号就行了比如npm i --save express@4.0.0（这个版本号瞎编的，实际中需要去官网看需要哪个版本），别人拿到项目后也不用一个个npm i xxx了可以直接运行npm i，npm会读取package.json文件里含有dependencies字段的内容并全部下载（除了dependencies，常用的还有devDependencies等，区别后面会讲） 好了我们继续，打开并修改www.js文件（加了相关注释） 1234567891011121314// 引入http模块const http = require('http');// 引入expressconst express = require('express');// 实例化expressconst app = express();// 用express的实例app创建一个服务const server = http.createServer(app);// 监听8080端口server.listen(8080);// 监听对根目录的get请求app.get('/', function (req, res, next) &#123; res.send('hello express');&#125;); 重启下服务器（ctrl+c终止后按↑就可以看到历史命令，找到node www后回车）每次对代码进行修改都要重启下服务器让内的代码进入内存（node会吧代码放到缓存，从缓存读取）然后刷新下浏览器，应该可以看到效果了，为了区分我把hello nodejs改成输出hello express。对比下两个www.js文件，其实就是`createServer`函数的参数用express的实例app代替了，而后面加了个app.get函数用于处理对“/”的get请求。至于get请求啥意思建议补一下http协议相关内容（找几篇博客看一下就好） 有了这个就方便多了，我们可以简单的往下加app.get或者post来处理不同的请求。www.js文件最后面加上 123456app.get('/user', function (req, res, next) &#123; res.send('user');&#125;);app.get('/timetable', function (req, res, next) &#123; res.send('timetable');&#125;); 重启node服务后访问localhost:8080/user和localhost:8080/timetable就可以看到效果了。然后是不是觉得重启每次改完代码都要重启下服务很烦？那就需要一些工具了。命令行执行npm i --save-dev supervisor。这回出现了个新东西--save-dev这个和上面出现过的--save是差不多的，只不过这样就把依赖写入package.json的devDependencies字段里面，表示这个依赖开发的时候才用得到，生产环境不需要。这样生产环境执行npm install --production就不会下载安装devDependencies下的东西。此时package.json文件 1234567891011121314151617&#123; "name": "timetable", "version": "1.0.0", "description": "", "main": "index.js", "scripts": &#123; "test": "echo \"Error: no test specified\" &amp;&amp; exit 1" &#125;, "author": "", "license": "ISC", "dependencies": &#123; "express": "^4.16.2" &#125;, "devDependencies": &#123; "supervisor": "^0.12.0" &#125;&#125; 这时候就可以使用supervisor www代替node www了，supervisor会自动监听文件，有改动就自动重启。然后我们发现supervisor这个单词好长，教练，有没有简单的命令？当然有。package.json有一个scripts字段，目前里面只有&quot;test&quot;: &quot;echo \&quot;Error: no test specified\&quot; &amp;&amp; exit 1&quot;，我们加上&quot;start&quot;: &quot;supervisor www&quot;,，现在package.json的scripts变成了这样 1234"scripts": &#123; "start": "supervisor www", "test": "echo \"Error: no test specified\" &amp;&amp; exit 1"&#125;, 然后就可以用npm run start代替supervisor www命令了，然后npm对start做了简化可以直接写npm start（具体关于这部分内容可以看这里），好了我们执行npm start 继续。随着项目变大，代码全部写在www文件会导致文件越来越大。是时候做一波代码分离了。首先吧app和www分离，后续代码基本不会修改www里面的内容了，就是app内容的修改（多写几个app.get等…)好，首先新建个app.js并把www.js里面关于app的代码分离出去，然后新建个bin文件夹，表示可执行目录，并把www.js文件拖进去，之后所有可执行文件都会放在bin目录（有些项目会有定时任务啥的要单独执行）... 1234567891011121314151617// app.js// 引入expressconst express = require('express');// 实例化expressconst app = express();app.get('/', function (req, res, next) &#123; res.send('hello express');&#125;);app.get('/user', function (req, res, next) &#123; res.send('user');&#125;);app.get('/timetable', function (req, res, next) &#123; res.send('timetable');&#125;);module.exports = app; 12345678910// bin/www.js// 引入http模块const http = require('http');// 引入app模块const app = require('../app');// 用express的实例app创建一个服务const server = http.createServer(app);// 监听8080端口server.listen(8080); 解释一下，这里把app相关的代码分离到了app.js，www.js的app就引入（因为app在父目录所以是`../app.js`）就好了。然后因为要提供给www文件引入所以app文件最后一行`module.exports = app;`就表示把app模块导出（这里是CommonJS规范的模块语法）。然后我们对bin/www.js文件做最后的改动，把端口号定义成常量然后server绑定了listening和error事件（用于报告错误，具体事件代码我是直接复制IDEA生成的express项目代码的）： 1234567891011121314151617181920212223242526272829303132333435363738// bin/www.js// 引入http模块const http = require('http');// 引入app模块const app = require('../app');// 定义端口常量const PORT = 8080;// 用express的实例app创建一个服务const server = http.createServer(app);// 监听8080端口server.listen(PORT);// 绑定成功监听端口事件server.on('listening', () =&gt; &#123; console.log('connect to port ' + PORT);&#125;);// 绑定错误事件server.on('error', (error) =&gt; &#123; if (error.syscall !== 'listen') &#123; throw error; &#125; const bind = typeof port === 'string' ? 'Pipe ' + port : 'Port ' + port; switch (error.code) &#123; case 'EACCES': console.error(bind + ' requires elevated privileges'); process.exit(1); break; case 'EADDRINUSE': console.error(bind + ' is already in use'); process.exit(1); break; default: throw error; &#125;&#125;); 然后我们可以不用管www文件了。把重点放到app文件，首先就是那几个app.get。之前演示已经知道了这个是绑定路由，根据前端请求的路径执行不同的函数实现后端数据处理逻辑。那么当项目越来越大以后，这样子的函数就会越来越多，都放到app.js显然不是很合适，是时候做一波路由分离了（这个名词我瞎编的0.0） 首先新建一个routes文件夹里面再新建index.js，user.js，timetable.js。然后把app.get相关代码转移过去。那么问题来了，怎么转移？之前把app从www转移是因为app完全是分离的代码，www里面的app可以导入app并且没有别的操作了。然而我们这里要把app.get转移出去的话index.js文件里面的app的确也可以导入，但是在routes/index.js文件写好的代码怎么导入到app.js文件呢？直接require么？那不是循环依赖了（routes/index.js require app，app.js又require routes/index.js）一个可行的方案是routes/index.js导出一个可执行函数并接受app作为参数，在app.js里面直接require并执行。示例代码 1234// app.jsapp.get('/', function (req, res, next) &#123; res.send('hello express');&#125;); 改成这样 12// app.jsrequire('./routes/index')(app); 123456// routes/index.jsmodule.exports = function (app) &#123; app.get('/', function (req, res, next) &#123; res.send('hello express'); &#125;);&#125;; 这么做的缺点这是把app整个根模块暴露给了项目的子模块，就有可能造成app模块的滥用导致影响别的代码，或者说，增强了模块的耦合。不过在express4.x开始引入了一个Router，于是分离路由有了新方式： 1234// app.jsapp.get('/', function (req, res, next) &#123; res.send('hello express');&#125;); 改成这样 123// app.jsconst index = require('./routes/index');app.use('/', index); 123456789// routes/index.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res) &#123; res.send('hello express');&#125;);module.exports = router; 这两种方式没什么本质的区别，app本质上也只是一个router而已，这里用了express.Router挂载路由，后面的get啊post啊都加到router上面，独立了一个路由的实例，具体有什么好处可以查看这篇文章浅谈 Express 4.0 Router 模块。这里有个地方需要注意的，现在无论app.get还是app.use还是router.get第一个参数都是“/”，看不出啥问题，但是要是换成“/user”就有区别了。下文细讲完整的分离后的代码： 123456789101112131415// app.js// 引入expressconst express = require('express');// 实例化expressconst app = express();// 引入别的路由模块const index = require('./routes/index');const user = require('./routes/user');const timetable = require('./routes/timetable');app.use('/', index);app.use('/user', user);app.use('/timetable', timetable);module.exports = app; 123456789// routes/index.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; res.send('hello express');&#125;);module.exports = router; 123456789// routes/timetable.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; res.send('timetable');&#125;);module.exports = router; 123456789// routes/user.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; res.send('user');&#125;);module.exports = router; 首先会发现index.js，timetable.js，user.js三个文件内部的get第一个参数都是“/”而不是“/user”啥的。看app.js用了app.use。就是使用中间件，具体这个啥意思目前可以理解为对第一个参数的路由的请求都由第二个参数的模块处理，比如app.use(&#39;/user&#39;,user)表示对/user路径的请求都会由user模块处理，那么/user的请求就是user模块的/对应的函数处理。在routes/user.js加一个路由： 123456789101112// routes/user.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; res.send('user');&#125;);router.get('/user', function (req, res, next) &#123; res.send('user2');&#125;);module.exports = router; 它本身在app.js就挂在到/user下了，内部再router.get(‘/user’)，所以对应到路由就是访问localhost:8080/user/user才能看到user2的输出。另外可以看到后面的function有三个参数，req，res，next。分别对应请求体，相应体和下一个函数。这里先解释下next这个参数修改routes/user.js 1234567891011121314// routes/user.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; req.user = 'aaa'; res.user = 'bbb'; next();&#125;);router.get('/', function (req, res, next) &#123; res.send('user name ' + req.user + res.user);&#125;);module.exports = router; 我们写了两个router.get(‘/‘)的函数，路由会先找到第一个执行后再找到第二个继续执行，所以访问localhost:8080/user就会显示user name aaabbb这里第一个/就可以理解为一种预处理。可以试试吧第一个/的函数的next()删了看看会发生什么，或者吧next()改成res.send(&#39;user&#39;)看看页面显示啥。 前后端分离前面写了那么多所谓的路由都是发送一串字符串，那我需要发送一个页面什么的怎么办呢？首先我们可以先尝试下修改index.js代码 123456789// routes/index.jsconst express = require('express');const router = express.Router();router.get('/', function (req, res, next) &#123; res.send('&lt;h1 style="color: red"&gt;hello express&lt;/h1&gt;');&#125;);module.exports = router; 访问http://localhost:8080看看页面输出了什么？是不是h1标签的大小以及样式都应用上了？其实我们发送的不是一个严格的html文件，但是浏览器当做html解析了。那我们要发送好看的首页带css样式再加点js效果那不是字符串拼接要累死人了？别慌express帮我们搞定了，而且只需要一点点代码。 1234567891011121314151617181920// app.js// 引入pathconst path = require('path');// 引入expressconst express = require('express');// 实例化expressconst app = express();// 引入别的路由模块const index = require('./routes/index');const user = require('./routes/user');const timetable = require('./routes/timetable');// 设置静态资源路径，express自动处理get请求app.use(express.static(path.join(__dirname, 'public')));app.use('/', index);app.use('/user', user);app.use('/timetable', timetable);module.exports = app; 就加了两行const path = require(&#39;path&#39;);和app.use(express.static(path.join(__dirname, &#39;public&#39;)));。然后根目录下新建public文件夹，里面再新建一个index.html，写入点简单的代码public/index.html 1234567891011121314151617181920&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt; &lt;meta charset="UTF-8"&gt; &lt;title&gt;timetable&lt;/title&gt; &lt;style&gt; h1 &#123; color: red; &#125; &lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;h1&gt;Time Table&lt;/h1&gt;&lt;script&gt; const h1 = document.querySelector('h1'); console.log(h1); h1.innerHTML += '123';&lt;/script&gt;&lt;/body&gt;&lt;/html&gt; 打开浏览器http://localhost:8080就可以看到效果啦。好了，解释下上面加的那些代码的意义，html文件就不解释了。。。主要是app.js里面加的const path = require(&#39;path&#39;);和app.use(express.static(path.join(__dirname, &#39;public&#39;)));第一句就是引入path依赖，它是nodejs自带的一个模块，会自动处理文件路径拼接的符号问题。比如用字符串拼接’a/‘+’/b就是’a//b’这个路径就会出错，但是path.join(‘a/‘, ‘/b’)结果就是’a\b’。（可以执行下routes/test.js看看输出结果）。这里顺便解释了后面那一句path.join(__dirname, &#39;public&#39;)的意思，就是把__dirname和public两个路径拼起来，至于__dirname就是项目路径名，nodejs全局定义的一个变量。然后继续往外解释express.static(path.join(__dirname, &#39;public&#39;))。外面的express.static就是设置静态文件路径，最后用app.use引入到项目，这句话整个的意思就是把public目录下的文件都设置成静态文件，所有的请求都先去public下找一遍，并且吧对/目录的请求绑定为public/index.html。所以加了这一句，nodejs对于网页发出的所有请求都会先去public目录找一遍，找到就返回那个静态文件，没找到就进入下一步逻辑。 举个例子，在public目录下新建一个123.txt，里面随便打点东西比如我打了“123”。打开浏览器，输入localhost:8080/123.txt我们发出了一个对123.txt的请求，可以看到浏览器显示了这个文件的内容。所以，对于浏览器的请求，node就会在app文件里面绑定的路由（app.use或者app.get app.post）一个个找下来，找到了就执行相关逻辑（或者直接返回静态文件）没找到就出错啦。所以我们在app.js最后加上两个错误处理的函数（放在app.js的最后一行的上面，虽然放最后也无所谓，但是这样美观一点0v0） 123app.use(function (req, res, next) &#123; res.send('404 NOT FOUND！')&#125;); 这个就是处理所有的路由都没有找到的情况，这里使用了use没有第一个参数，直接就是一个function，表示所有的请求都会经过它。访问一个不存在的路由就可以看到效果，比如localhost:8080/aaa就可以看到页面显示“404 NOT FOUND！”，因为前面所有路由都没有匹配的话就会走到最后一个app.use（就是之前刚加的那个。）这个和app.use(express.static(path.join(__dirname, &#39;public&#39;)));是一样的，所有的请求都会经过那个中间件，而express.static()最后返回的也是一个类似于function (req, res, next)这样的函数。所以我们可以了解到，express的前后端分离核心就是这一句，把public目录设置成静态文件目录导致浏览器所有请求会优先去public文件夹下寻找对应的文件，找到就返回。那么public目录就是前端的目录，前端使用html文件编写，可以直接返回并由浏览器渲染。 前端前端基本结构前面已经讲了public目录就是静态文件目录也就可以理解为前端目录，并且已经新建了两个文件作为例子(index.html和123.txt)。现在我们把文件结构建完整。在public文件夹下面新建文件夹css,js,img,lib,font，（这里我为了让GitHub不忽略他所以某些文件夹加了个.gitkeep文件，因为GitHub会忽略空文件夹，虽然理论上里面放个什么文件都可以，但是大家还是约定成.gitkeep文件。）然后随便写点东西，先把一开始建立的index.html文件的css部分和js部分分离。在css文件夹下新建index.css，js文件夹下新建index.js。 1234/* public/css/index.css */h1 &#123; color: red;&#125; 1234// public/js/index.jsconst h1 = document.querySelector('h1');console.log(h1);h1.innerHTML += '123'; 分离css和js后的html文件public/index.html 123456789101112&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt; &lt;meta charset="UTF-8"&gt; &lt;title&gt;timetable&lt;/title&gt; &lt;link rel="stylesheet" href="css/index.css"&gt;&lt;/head&gt;&lt;body&gt;&lt;h1&gt;Time Table&lt;/h1&gt;&lt;script src="js/index.js"&gt;&lt;/script&gt;&lt;/body&gt;&lt;/html&gt; 这里我们可以看到&lt;link rel=&quot;stylesheet&quot; href=&quot;css/index.css&quot;&gt;这个link标签的href直接引入了相对路径下的css。因为这个请求发送实质是一个对 localhost:8080/css/index.css的GET请求，所以服务端直接处理并返回了静态文件。由此也可以进一步确定public目录下的所有文件就是一个前端的工程项目。后端会自动发送相应请求文件。 AngularJS：MVC前端结构建好了就进入开发了，前端框架选择的是AngularJS，先引入文件（因为后续要使用webpack所以这里我们采用网络上的文件引入，所以确保自己联网。）这里我们找了个AngularJS1.6.6版本的CDN引入。(CDN这里可以简单理解为别人存在网络上的文件我们可以直接用)。修改index.html文件（注：以下操作全部在public文件夹下，所以提到文件名都不说具体目录了，全部都在public或者它的子目录下的文件） 12345678910111213141516&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt; &lt;meta charset="UTF-8"&gt; &lt;title&gt;timetable&lt;/title&gt; &lt;link rel="stylesheet" href="css/index.css"&gt;&lt;/head&gt;&lt;body ng-app="app"&gt;&lt;h1&gt;Time Table&lt;/h1&gt;&lt;div&gt; &lt;input type="text" placeholder="双向绑定测试" ng-model="name"&gt;&#123;&#123;name&#125;&#125;&lt;/div&gt;&lt;script src="https://cdn.bootcss.com/angular.js/1.6.6/angular.js"&gt;&lt;/script&gt;&lt;script src="js/index.js"&gt;&lt;/script&gt;&lt;/body&gt;&lt;/html&gt; 修改index.js文件 1angular.module('app', []) 这里不会讲太多AngularJS的概念（可以参考官网或者哪天我有空了再写相关文章…）只会说一些我们实验室的开发流程。 首先AngularJS就是一个库，和JQuery一样的方式引入就好了，这里是script标签的src引入了一个网上的库。然后我们在body标签里面加了一个ng-app=&quot;app&quot;，然后js的代码angular.module(&#39;app&#39;, [])就申明了一个模块，angular.module这个函数接受两个参数，第一个是模块名，第二个是依赖名，第二个参数啥意思后面会介绍，现在因为我们没有别的依赖所以就让第二个参数为空数组。第一个参数的模块名就是对应页面上的ng-app的值，告诉AngularJS这个是一个模块名字叫做app(ng-app的等号后面的值)，所以页面上的ng-app后面的值要和js里面的angular.module的第一个参数一样，不然会出错。这里就叫app了。因为angular主要是单页应用的框架，所以只会有一个主html文件（就是包含head body等标签的html），别的代码都是以html片段的形式存在，在angular需要时会动态加载进来替换掉页面上的部分DOM结构。 解释完了我们写的代码，可以打开浏览器看看效果。我们发现在input框里输入的内容都会在后面出现。这里就是双向绑定的概念了，回到代码，发现我们在input框加入了ng-model=&quot;name&quot;，然后后面又加了{{name}}、ng-model的意思是把输入框的值和后面那个变量进行绑定{{}}的意思是把变量进行输出。这样input的内容有修改的话就可以实时在内存中变化了。同理要是内存中的变量变化了也会在input中显示（比如js代码修改了变量值）说说不解释概念还是废话了一堆.. 刚刚提到内存中的变量，可能对这个概念有点懵逼，现在我们加上controller，就会对这个概念有点了解了。修改index.html的一部分代码（就是div 标签上加了一个ng-controller=&quot;appCtrl&quot;） 123&lt;div ng-controller="appCtrl"&gt; &lt;input type="text" placeholder="双向绑定测试" ng-model="name"&gt;&#123;&#123;name&#125;&#125;&lt;/div&gt; 修改index.js，定义一个controller（就是那个.controller），名字为appCtrl（第一个参数，是个字符串），第二个参数就是这个controller的内容（是个函数）。 12345angular.module('app', []) .controller('appCtrl', function ($scope) &#123; $scope.name = 123; &#125;); ng-controller就是一个控制器，控制器是什么概念呢？简单理解下就是和页面绑定在一起的数据，比如页面里面的文字，输入框里面的内容，甚至是一个按钮的点击事件对应（绑定）的函数。为什么要叫控制器呢，因为这里的代码可以控制那些数据，并且可以和页面上的内容双向绑定。这样我们要得到页面上的数据就不用再通过获取结点，在获取内容的方式了，可以直接获取内存中的数据，也可以直接修改内存中的数据修改页面显示。比如上述代码就是通过修改内存中的name来修改页面中的name，所以打开localhost:8080/可以看到input输入框里面已经有一个123了。至于controller里面的$scope是啥？emmmmmm…继续简单理解下就是包含页面所有数据的一个对象，页面的所有变量的双向绑定的值都在这个$scope里面。 一句话总结controller：控制页面的数据。另外，页面的数据全部在$scope里面，$scope里面的东西在页面上也可以直接用。 为了加深理解，我们加个按钮显示数据。 1234&lt;div ng-controller="appCtrl"&gt; &lt;input type="text" placeholder="双向绑定测试" ng-model="name"&gt;&#123;&#123;name&#125;&#125; &lt;button ng-click="show()"&gt;show&lt;/button&gt;&lt;/div&gt; 12345678angular.module('app', []) .controller('appCtrl', function ($scope) &#123; $scope.name = 123; $scope.show = function () &#123; console.log($scope.name); &#125;; &#125;); 可以看到，在html里面我们在input下面加了一个button，并在标签里面加上了ng-click=&quot;show()&quot;这个ng-click就是angular的事件绑定的写法，至于这个show()自然就是绑定在click时间的函数啦，show这个函数在controller里面就是$scope.show，之前说了，页面的所有内容都是在$scope下的。然后我们在controller里面给它赋值了一个函数，函数log出$scope.name，现在打开控制台，点击按钮，就能看到控制台输出了123，我们修改input的内容，再按一下按钮，会发现控制台输出了我们填写的内容，这就是双向绑定的直观概念，它的好处也就显而易见了，对于表单啊什么的直接绑定到$scope里面了，点击按钮就可以发送请求给后端了，不用麻烦的一个个获取。 我们试试看吧 12345&lt;div ng-controller="appCtrl"&gt; &lt;input type="text" placeholder="用户名" ng-model="user.name"&gt;&lt;br/&gt; &lt;input type="password" placeholder="密码" ng-model="user.pwd"&gt;&lt;br/&gt; &lt;button ng-click="submit()"&gt;提交&lt;/button&gt;&lt;/div&gt; 1234567angular.module('app', []) .controller('appCtrl', function ($scope) &#123; $scope.submit = function () &#123; console.log($scope.user); &#125;; &#125;); 我们修改了设置了controller的那个div里面的内容，加了两个input用于输入用户名和密码，ng-mode我们绑定到了user的两个属性，button的click绑到了submit函数。注意，我们ng-model不是name和pwd，而是user.name和user.pwd，这样controller里面直接$scope.user就是包含了两个属性的对象，非常方便。在另外，页面的数据会自动存放在controller里面，我们不用在controller里面重新定义一遍就可以直接用（比如这里就是console.log($scope.user);我们在外面并没有也不需要定义$scope.user）现在看看效果。打开http://localhost:8080随便输入什么，点击提交，就可以看到控制台输出了user对象。当然我们也可以初始化一下，设置默认值，这个在调试阶段特别好用： 1234567891011angular.module('app', []) .controller('appCtrl', function ($scope) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); &#125;; &#125;); 刷新一下浏览器，可以看到两个输入框已经有内容了，在调试时就不用反复输入了。 好了，我们已经可以在控制台看到数据打印出来了，那么怎么发送到后端呢？这里就要用ajax了，ajax就是一个发送异步请求的东西而已，具体不详细讲，只说怎么用（具体内容可看我写的有关angular的内容，有空写的话。。。）在angular里面，提供了一个$http的服务用语发送请求。$http和$scope一样，都是一个angular提供的服务，需要作为controller的参数才能使用。 123456789101112angular.module('app', []) .controller('appCtrl', function ($scope, $http) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); $http.post('http://localhost:8080/user/login',$scope.user); &#125;; &#125;); 首先可以看到我们在controller的参数里面加入了$http,然后可以看到在submit函数里面加了一行$http.post(&#39;http://localhost:8080/user/login&#39;,$scope.user);。这一行表示向localhost:8080/user/login这个路径发送了一个post请求，请求的参数是$scope.user。好了，我们要在这里插入一点后端的内容。前端已经吧输入发过去了，后端怎么获取呢？还记得之前后端的router.get么，它接受get请求，事实上我们在浏览器上输入一个url就是一个get请求。现在要一个post，那么我们就加上对post的处理。修改根目录的routes文件夹下的user.js 123456789// routes/user.jsconst express = require('express');const router = express.Router();router.post('/login',function (req, res, next) &#123; console.log('login');&#125;);module.exports = router; 这里我们先输出一个login字符串来看看是不是请求到了这个接口（所谓的接口就是一个uri，比如这里就是http://localhost:8080/user/login）。点击submit，然后打开运行着npm start的那个命令行，可以看到在黑框框下输出了一个字符串“login”，说明我们成功请求到这个接口了，那么问题就来了，我们怎么获取到传过来的参数呢？express有很好的中间件机制，当然是使用别人提供的中间件啦！这里我们用body-parser这个中间件，它可以预先帮我解析请求中的请求体，并把post请求的参数设置到req.body里面。首先要安装，在命令行多按几次ctrl+c终止那个那个npm start的命令，然后执行npm i --save body-parser，安装完后去修改根目录下的app.js，吧依赖引入进来，并用app.use把body-parser配置好 123456789101112131415161718192021222324252627282930313233// app.js// 引入pathconst path = require('path');// 引入expressconst express = require('express');// 实例化expressconst app = express();// 引入body-parserconst bodyParser = require('body-parser');// 引入别的路由模块const index = require('./routes/index');const user = require('./routes/user');const timetable = require('./routes/timetable');// 解析json格式的post请求体，给req添加body属性app.use(bodyParser.json());// 中间件只解析urlencoded 请求体，并返回，只支持UTF-8编号文本// extend// ture-&gt;使用queryString库（默认） false-&gt;使用qs库。app.use(bodyParser.urlencoded(&#123; extended: false &#125;));// 设置静态资源路径，express自动处理get请求app.use(express.static(path.join(__dirname, 'public')));app.use('/', index);app.use('/user', user);app.use('/timetable', timetable);app.use(function (req, res, next) &#123; res.send('404 NOT FOUND！')&#125;);module.exports = app; 其实我们就加了这3行 123const bodyParser = require('body-parser');app.use(bodyParser.json());app.use(bodyParser.urlencoded(&#123; extended: false &#125;)); 第一行引入依赖没啥问题，后两行用app.use说明所有的请求都会经过他们，和设置静态页面一样的用法。至于后两行use里面啥意思，上面的文件也做了详细说明，这里，具体可以自行搜索相关资料进一步学习，这里我们暂时就知道怎么用就好了。然后我们的req.body里面应该已经有内容了。修改routes/user.js，把之前的login字符串改为req.body 12345678const express = require('express');const router = express.Router();router.post('/login',function (req, res, next) &#123; console.log(req.body);&#125;);module.exports = router; 好，继续运行npm start，打开浏览器按submit。在打开命令行的时候我们可以看到输出的不是login了，而是{ name: 123, pwd: 123 }这个对象，前端已经成功到后端了，那么后端怎么发送信息给前端呢？超简单res.send啊！不不不，我们的意思是，前端怎么收到呢？angular的$http早就帮我们搞定了这些。routes/user.js 123456789const express = require('express');const router = express.Router();router.post('/login',function (req, res, next) &#123; console.log(req.body); res.send('登陆成功!');&#125;);module.exports = router; public/js/index.js 123456789101112131415angular.module('app', []) .controller('appCtrl', function ($scope, $http) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); $http.post('http://localhost:8080/user/login',$scope.user) .then(function (res) &#123; console.log(res); &#125;) &#125;; &#125;); 后端部分就是加了一个res.send发送了一个字符串，前端部分可以看到我们在刚刚那个post下加了一个then，这里$http.post返回了一个promise，具体promise是啥可以百度也可以看我别的文章（哇，我都挖了几个坑了。。。慢慢填。。。）then接受两个参数，都是函数，第一个函数是返回成功时的情况，第二个函数就是返回错误是的情况。我们暂时先不考虑错误情况，直接then接受一个函数，然后把res作为参数并打印出来，可以看到控制台输出了好多东西。嘛。。。其实也就是一个对象啦，我们关注data这个字段就好了，可以看到data这个字段里面的东西就是后端返回的值。装个逼用一下es6的解构赋值（也不算啥新东西啦，es8都要出来了。。。） 1234$http.post('http://localhost:8080/user/login',$scope.user) .then(function (res) &#123; console.log(res); &#125;) 改为 1234$http.post('http://localhost:8080/user/login', $scope.user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) 再刷新浏览器点击提交，就可以看到控制台输出后端返回的字符串啦，是不是很简单~ 随着逻辑越来越复杂，在controller里面既要控制页面的数据，又要和后端交互处理请求相应，那代码就会越来越庞大，是事实再分离一波了！这时候就可以手写service了。service字面意思就是服务，就是提供一些通用的服务供controller调用，其实之前用到的$scope,$http都是服务，只不过它们是angular提供的服务，我们也可以自己定义服务。比如定义一个UserService服务，把和用户相关的比如登录注册重置密码的逻辑都放在这里供controller调用。 123456789101112131415161718192021// public/js/index.jsangular.module('app', []) .controller('appCtrl', function ($scope, UserService) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); UserService.login($scope.user); &#125;; &#125;) .service('UserService', function ($http) &#123; this.login = function (user) &#123; $http.post('http://localhost:8080/user/login', user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) &#125; &#125;); 这样虽然controller和service分开了，但是都放在一个文件还是觉得很乱，那我们进一步分离，首先删了public/js/index.js。因为我们要把它拆开。在public文件夹下新建app.js作为主要入口，然后在public/js下新建controller.js和service.jspublic/app.js 12345angular.module('app', [ 'app.controller', 'app.service']); public/js/controller.js 123456789101112angular.module('app.controller', []) .controller('appCtrl', function ($scope, UserService) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); UserService.login($scope.user); &#125;; &#125;); public/js/service.js 12345678910angular.module('app.service', []) .service('UserService', function ($http) &#123; this.login = function (user) &#123; $http.post('http://localhost:8080/user/login', user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) &#125; &#125;); 好了，分完了，就是把controller，service简单拆了一下，另外controller.js第一行的module名字取为“app.controller”，service的module名字取为“app.service”。然后在app.js里面的module的第二个参数去进行“依赖注入”，就是把module名字放进数组里就好啦。最后还有一步！把这些js引入到html里面（把index的引入删除在加上新加的三个文件的引入） 1234&lt;script src="https://cdn.bootcss.com/angular.js/1.6.6/angular.js"&gt;&lt;/script&gt;&lt;script src="js/controller.js"&gt;&lt;/script&gt;&lt;script src="js/service.js"&gt;&lt;/script&gt;&lt;script src="app.js"&gt;&lt;/script&gt; 好了运行http://localhost:8080看看效果吧（应该没什么效果，和原来一样的界面） webpack的加入可以看到，之前我们只是简单做了个代码分离，就要在index.html多加入三个script标签，以后万一要加上别的库文件又是一大堆script标签，然后要加上css的话继续一堆link。。。另外，每次写完代码都要刷新一遍网页也很烦，后端部分我们已经用supervisor自动重启服务器来减轻负担了，前端有没有自动刷新浏览器什么的工具啊？必须是有的，而且可以一起吧上面两个问题全部解决！ webpack就是这么一个工具，它会从一个（或多个）js文件开始，一次查找所有的依赖，并引入进来打包成一个js文件。它强大的加载器以及插件可以做到找到css依赖甚至编译less，scss，ts等语言并引入。还有一个叫做webpack-dev-server的东西可以实现热加载（就是自动刷新浏览器，甚至不刷新浏览器就可以修改页面）。它的工作全部都写入一个配置文件，并根据配置文件的内容，实现一定功能，比如最基础的查找js依赖，配置完加载器后可以加载css文件，编译less，scss，配置devServer后可以热更新，配置html插件后可以吧标签自动插入到html文件里面。 听得那么多都心动了吧？那我们开始吧！ 既然要用webpack，当然得先安装（下载）喽，就是一句npm i的事，但是我们要装的不只是webpack，还有一些别的东西（因为webpack要加载html等需要别的配置，所以需要相关加载器或者插件等）这里我们就一次性下完啦。。。npm i --save-dev webpack webpack-dev-server html-webpack-plugin open-browser-webpack-pluginnpm i --save-dev babel babel-core babel-loader babel-plugin-istanbul babel-preset-es2015npm i --save-dev css-loader less-loader postcss-loader sass-loader style-loadernpm i --save-dev file-loader html-loader istanbul-instrumenter-loader url-loader为了美观点我这里分成了4条，当然也可以写到一句里面。。第一条是webpack和热加载的服务器webpack-dev-server，后面两个是webpack的两个插件，第一个用于自动在html里面插入标签（当然它有更牛逼的功能，这里我们就用来插入标签了，就是打包后的script标签就是靠它加入到html文件的）第二个是自动打开浏览器的插件。第二条是js相关的编译加载器，第三条是css相关，最后一条是别的杂七杂八的加载器。。。因为webpack本身只是处理js中的模块依赖问题的，有了这些加载器后可以加载别的css等文件（还可以编译es6，编译less等） 好了装完后怎么让它们合作起来呢？根目录下新建一个webpack.config.js的文件（这是webpack的默认配置文件名，要是换别的名字需要在启动webpack时指定。用这个名就不用另外指定了）。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384// webpack.config.jsconst path = require('path');const HtmlWebpackPlugin = require('html-webpack-plugin');const OpenBrowserPlugin = require('open-browser-webpack-plugin');const webpack = require('webpack');// 定义了一些路径const APP_PATH = path.resolve(__dirname, 'public/app.js');const BUILD_PATH = path.resolve(__dirname, 'build');module.exports = &#123; // 入口，分为app 入口和提取插件库的入口 entry: &#123; app: APP_PATH, &#125;, // 输出文件路径和名字 output: &#123; path: BUILD_PATH, filename: 'bundle.js' &#125;, plugins: [ new HtmlWebpackPlugin(&#123; template: './public/index.html', inject: 'body', minify: false &#125;), //自动启动浏览器 new OpenBrowserPlugin(&#123;url: 'http://localhost:8081'&#125;) ], //dev 服务器 devServer: &#123; historyApiFallback: true, hot: true, inline: true, progress: true, contentBase: "./build", // dev server的根路径 port: 8081 &#125;, // 加载器 module: &#123; "loaders": [ &#123; test: /\.html$/, loader: 'html-loader' &#125;, &#123; test: /\.css$/, loader: 'style-loader!css-loader' &#125;, &#123; test: /\.less/, loader: 'style-loader!css-loader!less-loader' &#125;, &#123; test: /\.(png)|(jpg)|(gif)|(woff)|(svg)|(eot)|(ttf)$/, use: [ &#123; loader: "url-loader", options: &#123; limit: 50000, //小于50K的 都打包 name: "[hash:8].[name].[ext]", publicPath: "img/", //替换CSS引用的图片路径 可以替换成爱拍云上的路径 outputPath: "../img/" //生成之后存放的路径 &#125; &#125; ] &#125;, &#123; test: /\.js?$/, loader: 'babel-loader', include: APP_PATH, query: &#123; presets: ['es2015'] &#125; &#125; ] &#125;, devtool: 'source-map'&#125;; 我知道被甩一脸代码你的内心是拒绝的，emmm…毕竟webpack有很多东西，详细信息需要参考官网文档，这里只给出了一些简单的配置例子而已（我也掌握的不多，仅仅是能用的程度ORZ…）之前已经提到了webpack是依赖处理工具，所以开头配置就是entry字段，entry表示依赖的起点，表示js的根文件，也是它查找依赖的起点。然后有entry就有output，这就是编译完后的代码存放的位置，另外webpack通过插件和模块加载器来扩展功能，比如我们加了HtmlWebpackPlugin插件配置，就表示吧生成的代码作为资源引用插入到对应html的body标签，OpenBrowserPlugin插件就表示以默认浏览器自动打开对应url。关于这些插件的用法每个都有相关的官方文档可供参考，这些不是webpack自带的，需要开发者自行查找相关插件（一般你遇到的诡异的需求都有别人给你做好的插件可以用）。devServer字段就是配置webpack-dev-server的地方，后面的module就是模块的加载器了，比如webpack不认识html文件就需要用HTML加载器引入。这里就是简单提一下webpack算是抛砖引玉，要深入学习还是要去啃webpack官方文档啊~另外这里就是单纯的够用，比如现在babel有些更新也没怎么关注…一切使用以官方文档为准。 好了，配置文件都写完了是不是迫不及待要试一下？别急，有些地方也得改一改。首先当然是html文件删除所有的script，link标签。public/index.html就只剩下下面这坨光秃秃的代码了，看上去清爽了一点 123456789101112131415&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt; &lt;meta charset="UTF-8"&gt; &lt;title&gt;timetable&lt;/title&gt;&lt;/head&gt;&lt;body ng-app="app"&gt;&lt;h1&gt;Time Table&lt;/h1&gt;&lt;div ng-controller="appCtrl"&gt; &lt;input type="text" placeholder="用户名" ng-model="user.name"&gt;&lt;br/&gt; &lt;input type="password" placeholder="密码" ng-model="user.pwd"&gt;&lt;br/&gt; &lt;button ng-click="submit()"&gt;提交&lt;/button&gt;&lt;/div&gt;&lt;/body&gt;&lt;/html&gt; 至于我们删除部分的代码之前webpack配置的插件会吧打包后的代码自动引入的~下一步改造js，既然app.js定义了app模块和界面绑定，它显然就是起点啦 123456789101112// public/app.jsimport angular from 'angular';import controller from './js/controller';import service from './js/service';angular.module('app', [ controller, service]);import './css/index.css'; emmmmm….似乎我们把angular的scrip也删了，然后还忘记装了？那装一下吧。。。npm i --save angular。然后可以看到我们原来直接写在数组里的是字符串，现在是引入的内容。至于这个import只是一个es6的模块语法，表示从对应的文件引入某个变量或者函数。既然引入了，那对应的文件是不是要导出？没错，继续改造controller和service文件 12345678910111213//public/js/controller.jsexport default angular.module('app.controller', []) .controller('appCtrl', function ($scope, UserService) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.submit = function () &#123; console.log($scope.user); UserService.login($scope.user); &#125;; &#125;) .name; 1234567891011//public/js/service.jsexport default angular.module('app.service', []) .service('UserService', function ($http) &#123; this.login = function (user) &#123; $http.post('http://localhost:8080/user/login', user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) &#125; &#125;) .name; controller和service的修改完全一样就是加个导出而已，至于controller和service用到的angular，已经在app导入过了，你想再导入一遍也没啥问题，因为webpack会判断发现已经有了就不会重复导入。导出的语法就是一个export，至于default，就是默认导出，这个每个文件只能有一个，别人导入的时候就可以不用管有什么无脑导入就行了。至于更多的es6模块部分的内容建议阅读相关博客深入了解。（敲黑板！！es6是必修课！！）最后一步，启动命令行！当然先加个别名，可以少打点东西也好记，怎么加之前已经提过了，就是在package.json的scripts字段加一条&quot;dev&quot;: &quot;webpack-dev-server --history-api-fallback --hot --inline --progress --content-base ./build/ --port 8081&quot;因为之前装了好多依赖，所以现在package.json文件是这样的 123456789101112131415161718192021222324252627282930313233343536373839&#123; "name": "timetable", "version": "1.0.0", "description": "", "main": "index.js", "scripts": &#123; "start": "supervisor bin/www", "dev": "webpack-dev-server --history-api-fallback --hot --inline --progress --content-base ./build/", "test": "echo \"Error: no test specified\" &amp;&amp; exit 1" &#125;, "author": "", "license": "ISC", "dependencies": &#123; "angular": "^1.6.6", "body-parser": "^1.18.2", "express": "^4.16.2" &#125;, "devDependencies": &#123; "babel": "^6.23.0", "babel-core": "^6.26.0", "babel-loader": "^7.1.2", "babel-plugin-istanbul": "^4.1.5", "babel-preset-es2015": "^6.24.1", "css-loader": "^0.28.7", "file-loader": "^1.1.5", "html-loader": "^0.5.1", "html-webpack-plugin": "^2.30.1", "istanbul-instrumenter-loader": "^3.0.0", "less-loader": "^4.0.5", "open-browser-webpack-plugin": "0.0.5", "postcss-loader": "^2.0.8", "sass-loader": "^6.0.6", "style-loader": "^0.19.0", "supervisor": "^0.12.0", "url-loader": "^0.6.2", "webpack": "^3.8.1", "webpack-dev-server": "^2.9.4" &#125;&#125; 恩，终于结束了。。。转换的过程是痛苦的，结果是美好的，现在打开命令行进入项目根目录输入npm run dev，可以看到浏览器自动打开了，界面什么的和之前一样，我们可以随便修改点css代码看看效果，比如把颜色改为蓝色，然后回到浏览器就可以看到自动更新了。然而当我们点击提交却出错了，他说我们跨域了。跨域是因为webpack-dev-server自己开了一个服务器在8081端口，我们额后端是8080端口，所以就被拒绝了，我们只要在后端加几行就好了。打开根目录下的app.js文件，在这几行的上面 1234// app.jsapp.use('/', index);app.use('/user', user);app.use('/timetable', timetable); 加上这几行 123456789// app访问预处理中间件// 设置跨域请求许可以及返回内容的数据格式app.use(function (req, res, next) &#123; res.setHeader("Access-Control-Allow-Origin", "*"); //允许哪些url可以跨域请求到本域 res.setHeader("Access-Control-Allow-Methods", "GET,POST"); //允许的请求方法，一般是GET,POST,PUT,DELETE,OPTIONS res.setHeader("Access-Control-Allow-Headers", "x-requested-with,content-type,Token"); //允许哪些请求头可以跨域 next();&#125;); 然后在点击提交试试，应该就好了，这是nodejs常用的解决跨域的方式。 数据库MongoDB简介前后端都连上了，该到数据库了。前端发过来的用户名和密码，我们需要存到数据库，一个注册的功能通常都是这样，前端发送用户输入的数据到后端，后端存入数据库中，这样下次这个用户只需要登录就好，并且与他相关的数据也会保存在一块。 常用的数据库是关系数据库，常见的Oracle，SQL Server还有MySQL等都是关系数据库。但是面对互联网日益爆增的数据量，关系数据库逐渐显得力不从心，于是出现了一类新型的数据库。我们称为NoSQL，Not Only SQL。非关系型数据库。非关系数据库又有好多种，MongoDB属于其中的文档型数据库。它的优点是数据结构要求不严格，表结构可变，不需要像关系型数据库一样需要预先定义表结构（这句来自百度百科）。而且存储结构就是json的结构，非常适合与js协作。 MongoDB的安装不多说了就是一个数据库引擎而已。主要是启动会有点东西需要配置。而每次手动启动也很烦一般就把它设置为一个服务了然后开机自启就行。这里引用菜鸟教程的配置方式 管理员模式打开命令行窗口创建目录，执行下面的语句来创建数据库和日志文件的目录mkdir c:\data\dbmkdir c:\data\log创建配置文件创建一个配置文件。该文件必须设置 systemLog.path 参数，包括一些附加的配置选项更好。例如，创建一个配置文件位于 C:\mongodb\mongod.cfg，其中指定 systemLog.path 和 storage.dbPath。具体配置内容如下： 12345systemLog: destination: file path: c:\data\log\mongod.logstorage: dbPath: c:\data\db 通过执行mongod.exe，使用–install选项来安装服务，使用–config选项来指定之前创建的配置文件。“C:\mongodb\bin\mongod.exe” –config “C:\mongodb\mongod.cfg” –install启动MongoDB服务net start MongoDB关闭MongoDB服务net stop MongoDB移除MongoDB服务“C:\mongodb\bin\mongod.exe” –remove 闲每次开启服务麻烦可以去windows的“服务”里设置开机自启动 MongoDB可视化工具所谓可视化工具也可以称为数据库管理工具，就是可以直接在界面上以用户友好的方式查看数据库的内容。MongoDB的话我常用的是mongobooster和Robomongo，mongobooster支持数据库的json文件导入导出，但是界面上个人认为Robomongo更友好一点，所以我一般做简单的查询修改都是用Robomongo。至于这两个软件就自行寻找安装包安装啦，就是两个普通软件而已…mongobooster是收费的，不过免费部分的功能也够用了（反正我只是用来导入导出数据。。。） Robmongo打开后要创建连接，链接本地的数据库服务后才可以操作里面的内容，点击create，保持默认点击save就好了，你愿意的话save之前改一下name啥的也行，（我改成了localhost。。）另外MongoDB默认是没有密码的，所以安全方面要另外设置，这里只是入门就保持默认了，做项目要用MongoDB一定要设置密码哦。然后连上后里面只有system和test，这是默认的数据库别理它。。 mongoose库介绍前面配置了数据库和可视化软件，现在就要正式码代码啦。道理我都懂，所以我要怎么用js来操作数据库呢？emmmmm，在后端程序和数据库引擎之前我们需要一个桥梁，这就是数据库驱动，所以我们需要安装一个库来帮我们，这里我选的是mongoose这个库。执行npm i --save mongoose安装mongoose库。装完后我们需要创建一个链接，连上后就可以用js代码操作数据库了。链接很简单，无非就是端口号啊数据库名啊账号密码啊之类的对上就好了，因为默认没有账号密码所以就需要端口号和数据库名，这里我们选择time_table作为名字。然后我们总不能操作一次重新写一段连接数据库的代码吧？所以根目录下新建一个config文件夹里面新建config.js，写上以下内容 123456789//config/config.jsconst mongoose = require('mongoose');mongoose.connect('mongodb://localhost:27017/time_table');const db = mongoose.connection;db.on('error', console.error.bind(console, 'connection error:'));db.once('open', console.log.bind(console, 'connection success:'));exports.mongoose = mongoose; 这些代码均可以从mongoose的文档里找到，代码很简单，导入mongoose库后创建一个链接然后导出mongoose。我们之前不是说后端已经拿到数据了么，在routes/user.js里。我们只是简单的输出了一下前端的数据，现在我们要真正把它保存到数据库里面。以下的代码也从文档中可以找到。 123456789101112131415161718192021222324252627//routes/user.jsconst express = require('express');const router = express.Router();//导入已经配置好数据库连接的mongoose库const mongoose = require('../config/config').mongoose;//定义User这个"表"，noSQL术语叫做集合(collection)，调用mongoose.model并传入第一个参数是表名，第二个是表的结构//其中第二个参数作为表的结构使用mongoose.Schema定义，具体内容官网都有详细的文档。const userSchema = mongoose.Schema(&#123; name: String, pwd: String,&#125;);const User = mongoose.model('User', userSchema);router.post('/login',function (req, res, next) &#123; console.log(req.body); //根据定义的model新建一个对象并保存，第二个参数是保存成功后的回调函数 const user = new User(req.body); user.save(function (err, user) &#123; if (err) return console.error(err); console.log(user); res.send('登陆成功!'); &#125;);&#125;);module.exports = router; 可以看到我们对routes/user.js做了点手脚，首先导入配置好的mongoose库，然后新建了一个User的结构，另外提一句，mongoose.model第一个参数的复数形式才是真正的表名。前端数据发过来后我们用发过来的数据new了一个user并保存了。现在去页面上点击提交可以看到命令行里面输出了 12&#123; name: 123, pwd: 123 &#125;&#123; name: '123', pwd: '123', _id: 5a8d7d85148af21d2ca5baa7, __v: 0 &#125; 第一个自然就是前端传过来的数据，第二个就是保存的数据，MongoDB自动生成了_id和_v两个字段_id自然是唯一的值用于区分每个对象，这个值大家应该都不一样，至于这个值怎么产生的可以参考这个，总之你知道_id可以用于唯一区分数据库里一条数据就好了。_v是版本号，具体用法可以看官方文档。然后我们打开Robmongo可以看到里面多了”time_table”这个，如果没有的话右键连接名（就是侧栏最上面那个，名字取决于你创建时的名字，我的是localhost，默认是New Connection）找到第二个Refresh，刷新完就有了。然后可以看到下面有Collections，Functions，Users。展开Collections，可以看到users，双击就能看到里面的数据了，可以看到这样的内容 1234567/* 1 */&#123; "_id" : ObjectId("5a8d7a29f54eab2accaca062"), "name" : "123", "pwd" : "123", "__v" : 0&#125; 去网页上多点几下提交再打开Robmongo就变成这样了（没变化的话按f5刷新） 1234567891011121314151617181920212223/* 1 */&#123; "_id" : ObjectId("5a8d7a29f54eab2accaca062"), "name" : "123", "pwd" : "123", "__v" : 0&#125;/* 2 */&#123; "_id" : ObjectId("5a8d7d85148af21d2ca5baa7"), "name" : "123", "pwd" : "123", "__v" : 0&#125;/* 3 */&#123; "_id" : ObjectId("5a8d840e148af21d2ca5baa8"), "name" : "123", "pwd" : "123", "__v" : 0&#125; 可以看到我们每次点一下提交就执行了一次保存，又因为主键id是自动生成的所以name和pwd重复它也会保存，如果我们要让name不能重复，那这些逻辑都需要自己代码实现，比如先查询一下数据库看看name有没有被占用，没有的话就保存，有的话就返回一个错误信息。我们可以对login接口做如下修改： 123456789101112131415161718192021router.post('/login', function (req, res, next) &#123; console.log(req.body); User.findOne(&#123;'name': req.body.name&#125;, function (err, user) &#123; if (err) &#123; console.log(err); res.send('数据库错误'); return; &#125; if (user) &#123; res.send('用户已存在'); &#125; else &#123; //根据定义的model新建一个对象并保存，第二个参数是保存成功后的回调函数 const user = new User(req.body); user.save(function (err, user) &#123; if (err) return console.error(err); console.log(user); res.send('登陆成功!'); &#125;); &#125; &#125;);&#125;); User.findOne就是去users这个collection里面查找一个，找到或者没找到都会返回，找到了后回调函数的第二个参数就是user。没找到第二个参数就是null。所以如果user不是null就返回用户已存在，否者就保存。可以去网页上试验一下，点击提交可以看到控制台输出了用户已存在，把name改成1234再点提交就输出登录成功了。emmm…仔细想想这怎么会是登录呢，这明明应该是注册的逻辑嘛~那我们就做简单的修改，把它变成register就好了~上面的/login改成/register，在新建一个login的逻辑。 123456789101112131415161718192021222324252627282930313233343536router.post('/login', function (req, res, next) &#123; User.findOne(req.body, function (err, user) &#123; if (err) &#123; console.log(err); res.send('数据库错误'); return; &#125; if (user) &#123; res.send('登陆成功!'); &#125; else &#123; res.send('账号或密码错误!'); &#125; &#125;);&#125;);router.post('/register', function (req, res, next) &#123; console.log(req.body); User.findOne(&#123;'name': req.body.name&#125;, function (err, user) &#123; if (err) &#123; console.log(err); res.send('数据库错误'); return; &#125; if (user) &#123; res.send('用户已存在'); &#125; else &#123; //根据定义的model新建一个对象并保存，第二个参数是保存成功后的回调函数 const user = new User(req.body); user.save(function (err, user) &#123; if (err) return console.error(err); console.log(user); res.send('登陆成功!'); &#125;); &#125; &#125;);&#125;); 这样才像样嘛，注册才需要写入数据库，登录只要判断有没有这儿数据就行了，有就说登陆成功没有就说账号或者密码错误。关注login接口的这一行User.findOne(req.body, function (err, user) {...})第一个参数直接就是req.body。因为我们定义的数据结构和前端传过来的一样所以就这么写，完整的写法是这样的：User.findOne({name:req.body.name, pwd:req.body.pwd}, function (err, user) {...})（在这里也是多此一举的写法。。。）。findOne的第一个参数就是查找条件，只有完全满足才行。api文档看这里 改完后端那顺便前端也改了呗~public/index.html 123456&lt;div ng-controller="appCtrl"&gt; &lt;input type="text" placeholder="用户名" ng-model="user.name"&gt;&lt;br/&gt; &lt;input type="password" placeholder="密码" ng-model="user.pwd"&gt;&lt;br/&gt; &lt;button ng-click="login()"&gt;登录&lt;/button&gt; &lt;button ng-click="register()"&gt;注册&lt;/button&gt;&lt;/div&gt; 123456789101112131415//public/js/controller.jsexport default angular.module('app.controller', []) .controller('appCtrl', function ($scope, UserService) &#123; $scope.user = &#123; name: 123, pwd: 123 &#125;; $scope.login = function () &#123; UserService.login($scope.user); &#125;; $scope.register = function () &#123; UserService.register($scope.user); &#125;; &#125;) .name; 1234567891011121314151617//public/js/service.jsexport default angular.module('app.service', []) .service('UserService', function ($http) &#123; this.login = function (user) &#123; $http.post('http://localhost:8080/user/login', user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) &#125;; this.register = function (user) &#123; $http.post('http://localhost:8080/user/register', user) .then(function (&#123;data&#125;) &#123; console.log(data); &#125;) &#125; &#125;) .name; 改动很简单，就是原来提交的按钮改成了两个按钮，一个登陆一个注册。对应的controller，service也改成了登录，注册的逻辑。当然，真实应用的注册应该不止这两个字段，登录和注册的内容应该也是不一样的。这里就一切从简了。 细心的童鞋应该发现了，routes/user.js里面除了一个登陆一个注册还有一些诡异的代码，就是定义数据结构啊之类的。如下 12345678910//导入已经连接好数据库的mongoose库const mongoose = require('../config/config').mongoose;//定义User这个"表"，noSQL术语叫做集合(collection)，调用mongoose.model并传入第一个参数是表名，第二个是表的结构//其中第二个参数作为表的结构使用mongoose.Schema定义，具体内容官网都有详细的文档。const userSchema = mongoose.Schema(&#123; name: String, pwd: String,&#125;);const User = mongoose.model('User', userSchema); 这些代码定义了users集合的数据结构，并且User这个模型(model)也是和数据库直接绑定的，就是说我们要通过这个User来操纵users这个集合。所以别的地方要用users里面的数据就要获取到User的实例，那么我们就要吧User导出，但是这样是不合理的，因为这个文件只是一个路由，User相关的定义应该放到别的地方统一定义。所以在根目录下新建model文件夹，并新建User.js吧模型定义的相关代码移出去。 12345678910111213141516//model/User.js//导入已经连接好数据库的mongoose库const mongoose = require('../config/config').mongoose;//定义User这个"表"，noSQL术语叫做集合(collection)，调用mongoose.model并传入第一个参数是model名，第二个是表的结构//其中第二个参数作为表的结构使用mongoose.Schema定义，具体内容官网都有详细的文档。（比如数据类型之类的）//第三个参数是集合名，省略的话默认会使用第一个参数的全小写的复数形式（所以这个例子写不写都一样）const userSchema = mongoose.Schema(&#123; name: String, pwd: String,&#125;);const User = mongoose.model('User', userSchema, 'users');// const User = mongoose.model('User', userSchema); 和上面一行效果一样// const User = mongoose.model('User', userSchema, 'user'); 这么写集合名就是user了exports.User = User; 1234567891011121314151617181920212223242526272829303132333435363738394041424344//routes/user.jsconst express = require('express');const router = express.Router();const User = require('../model/User').User;router.post('/login', function (req, res, next) &#123; User.findOne(req.body, function (err, user) &#123; if (err) &#123; console.log(err); res.send('数据库错误'); return; &#125; if (user) &#123; res.send('登陆成功!'); &#125; else &#123; res.send('账号或密码错误!'); &#125; &#125;);&#125;);router.post('/register', function (req, res, next) &#123; console.log(req.body); User.findOne(&#123;'name': req.body.name&#125;, function (err, user) &#123; if (err) &#123; console.log(err); res.send('数据库错误'); return; &#125; if (user) &#123; res.send('用户已存在'); &#125; else &#123; //根据定义的model新建一个对象并保存，第二个参数是保存成功后的回调函数 const user = new User(req.body); user.save(function (err, user) &#123; if (err) return console.error(err); console.log(user); res.send('登陆成功!'); &#125;); &#125; &#125;);&#125;);module.exports = router; 到这里，前端后端数据库都连上了，代码也很清晰了。可以说我们的基础部分已经完成了。整个教程也到了尾声，本来打算介绍下测试，但是考虑到Angularjs即将被弃用就不打算再打一遍了，而Angular的cli配了一个开箱即用的测试框架，所以这部分内容以后写Angular相关的时候再详细介绍吧。至于开头说的time table这个项目，我是之前打过一遍的，功能也基本完整（还有测试相关代码）。这次写教程又根据文章节奏打了下，所以就到最简单的登录注册。并用commit记录内容。有心学习的同鞋可以去github的commit记录上一步步对照内容看下来。 项目代码 根据上面内容写的代码 timetable 文初说的完整的项目代码 timetable_fkq]]></content>
      <categories>
        <category>MEAN</category>
      </categories>
      <tags>
        <tag>front-end</tag>
        <tag>MEAN</tag>
        <tag>Nodejs</tag>
        <tag>AngularJS</tag>
        <tag>Express</tag>
        <tag>MongoDB</tag>
        <tag>back-end</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[web study]]></title>
    <url>%2F2017%2F03%2F30%2Fweb-study%2F</url>
    <content type="text"><![CDATA[大二前端课程的作业 我的作业实验1： table布局实验2： js事件练习实验3： div布局实验4： flex布局实验5： 网格语义综合排版练习实验6： JQuery基本练习实验7： Bootstrap栅格练习实验9： 百度地图基本练习期末project]]></content>
      <tags>
        <tag>front-end</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[hexo新建文章]]></title>
    <url>%2F2017%2F03%2F24%2Fhexo%E6%96%B0%E5%BB%BA%E6%96%87%E7%AB%A0%2F</url>
    <content type="text"><![CDATA[hexo常用命令相关笔记和使用说明转自百度经验(部分修改) hexo发布文章过程： hexo n “文章标题” source/_posts找“文章标题.md”文件并编写 hexo s 运行服务器 打开localhost:3000预览(默认是4000，修改端口在node_modules/hexo-server/index.js 第8行) hexo g 生成静态文件 hexo d 发布 hexo常用简写： 1234hexo n == hexo newhexo g == hexo generatehexo s == hexo serverhexo d == hexo deploy 命令行中输入： 1hexo new "new article" 之后在source/_posts目录下面，多了一个new-article.md的文件。 打开之后我们会看到： 123title: new articledate: 2014-11-01 20:10:33tags: 文件的开头是属性，采用统一的yaml格式，用三条短横线分隔。下面是文章正文。文章的正文支持markdown格式，建议你先学习一下它的语法。markdown不像html似的一大堆标签，很简单，只有几个符号。新建、删除或修改文章后，不需要重启hexo server，刷新一下即可预览。 属性文章可以拥有如下属性： Setting Description Default layout Layout post或page title 文章的标题 ate 创建日期 文件的创建日期 pdated 修改日期 文件的修改日期 omments 是否开启评论 true ags 标签 ategories 分类 ermalink url中的名字 文件名 动态博客中通过发布文章页面设置的各种属性，在hexo里要这样设置。 分类和标签例如： 12345678categories:- 日记tags:- Hexo- node.js 摘要同wordpress一样，&lt;!--more--&gt;之上的内容为摘要。 layout如果你修改了layout，在scaffolds文件夹里一定要有名字对应的模版文件，否则会采用默认模版。 文件名在配置文件中的new_post_name项可以设置文件名，默认为:title，也就是你在命令行输入的名字。 文件名可以为下面几个变量和字符串常量的任意组合： Variable Description :title Escaped title (lower case and replace spaces with dash) :year Created year (4-digit) :month Created month (2-digit) :i_month Created month (Without leading zeros) :day Created day (2-digit) :i_day Created day (Without leading zeros) 草稿草稿相当于很多博客都有的“私密文章”功能。 1hexo new draft "new draft" 会在source/_drafts目录下生成一个new-draft.md文件。但是这个文件不被显示在页面上，链接也访问不到。也就是说如果你想把某一篇文章移除显示，又不舍得删除，可以把它移动到_drafts目录之中。 如果你希望强行预览草稿，更改配置文件： 1render_drafts: true 或者，如下方式启动server： 1hexo server --drafts 下面这条命令可以把草稿变成文章，或者页面： 1hexo publish [layout] &lt;filename&gt;]]></content>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2F2017%2F03%2F23%2Fhello-world%2F</url>
    <content type="text"><![CDATA[hexo hello world Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post1hexo new "My New Post" More info: Writing Run server1hexo server More info: Server Generate static files1hexo generate More info: Generating Deploy to remote sites1hexo deploy More info: Deployment]]></content>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
